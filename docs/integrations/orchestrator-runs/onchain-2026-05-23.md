# On-chain orchestrator — 2026-05-23

## Invocation 1 — 2026-05-23T00:00:00Z

**Iteration:** 1 of 1  
**Elapsed:** ~15m  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path  
**M3 priority:** seller enrichment path, staging checklist gaps, honest buy gates (no fake on-site buy)  
**Global in-flight fix workers (before spawn):** ~10 — segment seg-bg-20..24 (5), security sec-bg-1..5 (5); headroom **14/24**

---

### Audit summary

**BFF tx routes** (`app/api/trade/tx/`):

| Route | Origin gate | Write gate | Wallet challenge | SDK writePath | Notes |
|-------|-------------|------------|------------------|---------------|-------|
| `buy` | yes | yes | yes | SDK fill + `takerBroker` | Green |
| `list` | yes | yes | yes | SDK list | Green |
| `delist` | yes | yes | yes | SDK delist via `resolveDelistWritePath` | Green (G4 fixed 2026-05-22) |
| `bid` | yes | yes | yes | REST + `makerBroker` default | Green |
| `cancel-bid` | yes | yes | yes | REST | Green |
| `challenge` | yes (security inline 2026-05-23) | n/a | issues challenges | n/a | Green |

**SDK / BFF libs:**

- `lib/onchain/clients/tensor-tcm.ts` — `buildListTransaction`, `buildFillTransaction`, `buildDelistTransaction`; write-gated
- `lib/onchain/clients/tensor-tcm-sdk.ts` — fill attaches `takerBroker` via `getSvfBrokerPubkey()`
- `lib/onchain/tensor-tx-bff.ts` — origin gate, write gate, wallet challenge, writePath resolvers
- `lib/onchain/clients/tensor-fees.ts` — `attachBrokerFeeAccounts` **stub** (TC-075); blocked on broker PDA + fees IDL
- `lib/onchain/fees.ts` — `getSvfBrokerPubkey`, `previewFeeSplit`; `/api/ops/fee-preview`

**Client hooks / modals:**

- `use-tensor-buy` — `writePath=sdk`, `canBuyOnChain` requires seller **or** listState + settlement mode
- `use-tensor-list` — `buildTensorListTxSearchParams` always `writePath=sdk` (G1/G3 fixed 2026-05-22)
- `use-tensor-delist` — `writePath=sdk`
- `use-tensor-tx` — wallet challenge flow wired when `TRADE_TX_REQUIRE_WALLET_CHALLENGE` (G11 fixed)
- `place-offer-modal` — uses `resolveTradeListingMint` (G2 fixed 2026-05-22)
- `map-listing.ts` — **gap:** `mint: listing.id` ignores `resolveTradeListingMint` for cert-keyed rows
- `nft-card.tsx` — honest seller-meta gate; missing write/wallet disabled state on grid Buy

**Write gates:**

- Server: `assertTensorTradeWriteEnabled()` on all tx routes
- Client: `isTradeWriteEnabledClient()` in modals, trade panel, hooks
- `.env.example`: `TENSOR_TRADE_WRITE_ENABLED=false` (prod default)
- **Inline (this tick):** `tradeWriteDisabledTooltip()` copy clarifies enrichment + ops sign-off requirement

**Seller enrichment (M3):**

- `lib/partner-listings.ts` — `enrichPartnerListingsWithTensor` merges `sellerWallet` + `listState`
- Buy path blocked at UI when enrichment missing (`tradeListingHasOnChainBuyMetadata`, `canBuyOnChain`)
- **Gap:** mapper still sends cert id as mint to BFF when vaultedUrl holds mint — breaks staging fill for cert-keyed CC rows

**Staging checklist** (`docs/trade-staging-checklist.md`):

- Pre-flight: all `[ ]` — partner ingest, enrichment, wallet, staging SOL **operator**
- Broker PDA: all registration steps `[ ]` — **operator-blocked**, no auto-fix spawn
- Staging verification steps 1–6: unchecked — requires staging deploy + first fill record
- First fill template: [staging-first-fill-record-template.md](./staging-first-fill-record-template.md) — **operator-blocked**

**Tx tests (this invocation):**

- Initial `npm test -- tests/...` — **mis-invoked** (`run-tests.ts` ignores positional paths; ran full suite **558 pass / 6 fail** in ~16m — failures are non–on-chain lane; QA owns full suite)
- Focused re-run: `npx tsx --test tests/api-trade-tx.test.ts tests/api-trade-tx-guardrails.test.ts tests/tensor-tcm.test.ts` — **73 pass / 0 fail** (~95s)

**Operator blockers (log only — no spawn):**

| Blocker | Status | Notes |
|---------|--------|-------|
| Broker fee PDA registration | OPEN | Treasury `2oRZe7z9Jx3rpoUWuidjGhpX9mxxhtps2JqQtdxLfHwg`; Fees program `TFEEgwDP6nn1s8mMX2tTNPPz8j2VomkphLUmyxKm17A` |
| First staging fill record | OPEN | Template unfilled; write flags off prod |
| Whitelist CC + Phygitals mints (P2-INT-OC08) | OPEN | `collectionMint: null` in collections config |

**Prod contract:** `.env.example` keeps `TENSOR_TRADE_WRITE_ENABLED=false`.

---

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G1 | `mapTradeListingToTensorNft` uses `listing.id` not `resolveTradeListingMint` | P0 | `map-listing.ts` | spawn oc-bg-1 |
| G2 | `trade-panel` `collectionMints` from `listing.id` only | P1 | `trade-panel.tsx` | spawn oc-bg-2 |
| G3 | Item detail `ListForSaleModal` mint=`listing.id` | P1 | `trade-item-detail-client.tsx` | spawn oc-bg-3 |
| G4 | Grid Buy missing write/wallet honest gates | P1 | `nft-card.tsx` | spawn oc-bg-4 |
| G5 | P2-INT-T16 post-buy verify hook not wired | P1 | `use-tensor-buy.ts` | spawn oc-bg-5 |
| G6 | Broker PDA registration on Tensor Fees | P0 | ops | deferred (operator) |
| G7 | Staging checklist + first fill record | P0 | ops/staging | deferred (operator) |
| G8 | `attachBrokerFeeAccounts` stub (TC-075) | P1 | `tensor-fees.ts` | deferred until broker PDA + IDL |
| G9 | P2-INT-OC08 whitelist mints on TL1ST | P2 | `lib/onchain/collections.ts` | deferred |
| G10 | P2-INT-T12 vault list tx staging verify | P2 | staging | deferred (operator) |

---

### Spawned workers

| Worker | Gap ID | Task title | Agent ID | Status |
|--------|--------|------------|----------|--------|
| oc-bg-1 | G1 | On-chain — map-listing mint resolve | `70c1d80e-64d9-4182-9a86-5e22388cdf59` | spawned (not awaited) |
| oc-bg-2 | G2 | On-chain — trade-panel collectionMints | `6a7cf153-516e-49f9-81ca-878732418893` | spawned (not awaited) |
| oc-bg-3 | G3 | On-chain — item detail list mint | `0bdb9115-1f5c-4cee-a0e8-4d766b76d764` | spawned (not awaited) |
| oc-bg-4 | G4 | On-chain — nft-card buy gates | `20bf3a9a-5139-4d1e-8e3e-32492ea64f80` | spawned (not awaited) |
| oc-bg-5 | G5 | On-chain — buy verify hook stub | `2178b290-60cb-417a-ba7f-b4f84b452f5a` | spawned (not awaited) |

**Spawned this iteration:** 5 / 5 lane budget  
**Global in-flight after spawn:** ~15 (segment 5 + security 5 + onchain 5)

---

### Inline fixes (orchestrator)

| File | Change |
|------|--------|
| `lib/trade/trade-modal.ts` | `tradeWriteDisabledTooltip()` — honest copy: writes off until staging ops + Tensor seller enrichment |

---

## End of invocation 1

**Total iterations (today):** 1  
**Total spawned (invocation 1):** 5  
**Global cap at stop:** ~15/24 in-flight (headroom for ui/backend/qa lanes)  
**Green criteria met:** partial — BFF + SDK routes green; client mint mapping + grid gates in flight; **staging checklist incomplete**; broker PDA ops pending  
**Recommend next lane:** **backend** (seller enrichment ingest) or **qa** (focused tx test re-run) on next meta tick  
**Operator action needed:** Broker PDA registration, staging write flags, first mainnet fill dry-run per [trade-staging-checklist.md](../../trade-staging-checklist.md)

---

## Invocation 2 — 2026-05-23T11:05:00Z

**Iteration:** 1 of 1  
**Elapsed:** ~10m  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path  
**M3 priority:** seller enrichment path, staging checklist gaps, honest buy gates  
**Global in-flight fix workers (before spawn):** **25** — segment 5, ui 5, backend 5, onchain 5, security 5 — **cap reached (≥ 24)**

---

### Audit summary

**Re-check vs invocation 1 workers (not awaited):**

| Gap | Status | Notes |
|-----|--------|-------|
| G1 `mapTradeListingToTensorNft` mint | **FIXED** | `resolveTradeListingMint(listing) ?? listing.id` in `map-listing.ts` |
| G2 `trade-panel` collectionMints | **FIXED** | `resolveTradeListingMint` in `collectionMints` useMemo |
| G3 item detail list mint | **PARTIAL → inline fixed** | `ListForSaleModal` still had `mint={listing.id}`; orchestrator set `onChainMint` |
| G4 grid Buy honest gates | **FIXED** | `nft-card.tsx` — `tradeWriteDisabledTooltip`, `resolveOnChainBuyBlockReason`, `canBuyOnChain` |
| G5 post-buy verify hook | **FIXED (stub)** | `logTensorFillSignatures` wired in `use-tensor-buy.ts`; format-only, no RPC |

**BFF tx routes:** unchanged green — buy/list/delist/bid/cancel-bid/challenge all origin + write + wallet challenge gated.

**Tx tests:** invocation 1 focused run **73 pass / 0 fail** (`api-trade-tx`, `api-trade-tx-guardrails`, `tensor-tcm`). Invocation 2 re-run **73 pass / 0 fail** (~184s test time, ~11m wall; exit 0).

**Spawn decision:** **`cap reached (concurrency cap)`** — global in-flight **25/24**; **0 workers spawned** this iteration.

---

### Gaps (remaining)

| ID | Gap | Severity | Owner | Action |
|----|-----|----------|-------|--------|
| G6 | Broker fee PDA registration on Tensor Fees | P0 | ops | operator-blocked |
| G7 | Staging checklist + first fill record unfilled | P0 | ops/staging | operator-blocked |
| G8 | `attachBrokerFeeAccounts` stub (TC-075) | P1 | `tensor-fees.ts` | deferred until broker PDA + fees IDL |
| G9 | P2-INT-OC08 whitelist mints (`collectionMint: null`) | P2 | `lib/onchain/collections.ts` | deferred |
| G10 | Full RPC post-fill verify (P2-INT-T16 follow-up) | P2 | `tensor-fill-verify.ts` | deferred until staging fill |

---

### Spawned workers

| Worker | Gap ID | Task title | Agent ID | Status |
|--------|--------|------------|----------|--------|
| — | — | cap reached (25/24 in-flight) | — | skipped |

**Spawned this iteration:** 0 / 5 lane budget  
**Global in-flight after spawn:** 25 (unchanged)

---

### Inline fixes (orchestrator)

| File | Change |
|------|--------|
| `components/trade/trade-item-detail-client.tsx` | `ListForSaleModal` `mint={onChainMint}` via `resolveTradeListingMint(listing) ?? listing.id` |

---

## End of invocation 2

**Total iterations (today):** 2  
**Total spawned (invocation 2):** 0 (cap reached)  
**Green criteria met:** partial — BFF + SDK routes green; client mint mapping largely fixed; **staging checklist incomplete**; broker PDA ops pending  
**Recommend next lane:** **qa** (focused tx test re-run + build unlock) or **backend** (seller enrichment coverage) on next meta tick  
**Operator action needed:** Broker PDA registration, staging write flags, first mainnet fill dry-run per [trade-staging-checklist.md](../../trade-staging-checklist.md)

---

## Invocation 3 — 2026-05-23T12:10:00Z (meta tick 3)

**Iteration:** 1 of 1  
**Elapsed:** ~12m  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path  
**M3 priority:** seller enrichment path, staging checklist gaps, honest buy gates  
**Global in-flight fix workers (before spawn):** journal nominal **25** (segment 5, ui 5, backend 5, onchain 5, security 5 — all `spawned (not awaited)`); **code-adjusted ~5** after inv1–2 landings (onchain oc-bg-1..5, backend be-bg-1..5, security sec-bg-1..5, ui ui-bg-1..5 verified complete; segment seg-bg-20..24 still nominal) — headroom **~19/24**

---

### Audit summary

**Re-check vs invocation 2 (oc-bg-1..5 complete, not re-spawned):**

| Gap | Status | Notes |
|-----|--------|-------|
| G1 map-listing mint resolve | **FIXED** | `resolveTradeListingMint(listing) ?? listing.id` |
| G2 trade-panel collectionMints | **FIXED** | mint resolve in useMemo |
| G3 item detail list mint | **FIXED** | `ListForSaleModal mint={onChainMint}` |
| G4 grid Buy honest gates | **FIXED** | `nft-card.tsx` write/wallet/seller-meta gates |
| G5 post-buy verify hook | **FIXED (stub)** | `logTensorFillSignatures` in `use-tensor-buy.ts` |

**BFF tx routes:** unchanged green — buy/list/delist/bid/cancel-bid/challenge origin + write + wallet challenge gated.

**Client honest buy gates:** `buy-now-modal`, `trade-item-detail-client`, `nft-card`, `trade-panel` sweep all wire `canBuyOnChain` + `resolveOnChainBuyBlockReason`. **New gap:** sweep hint copy conflates partner-site rows with on-chain rows missing Tensor seller enrichment.

**Seller enrichment (M3):** `enrichPartnerListingsWithTensor` merges `sellerWallet` + `listState`; buy blocked at UI when metadata missing. Cert-keyed mint resolve **green** in mapper; enrichment merge tests need cert+Tensor path coverage.

**Tx tests (this invocation):**

- `npx tsx --test tests/api-trade-tx.test.ts tests/api-trade-tx-guardrails.test.ts tests/tensor-tcm.test.ts tests/map-listing.test.ts tests/tensor-fill-verify.test.ts` — **82 pass / 0 fail** (~139s)

**Operator blockers (log only — no spawn):**

| Blocker | Status | Notes |
|---------|--------|-------|
| Broker fee PDA registration | OPEN | Treasury `2oRZe7z9Jx3rpoUWuidjGhpX9mxxhtps2JqQtdxLfHwg`; Fees program `TFEEgwDP6nn1s8mMX2tTNPPz8j2VomkphLUmyxKm17A` |
| First staging fill record | OPEN | Template unfilled; prod write flags off |
| Whitelist CC + Phygitals mints (P2-INT-OC08) | OPEN | `collectionMint: null` in collections config |

**Prod contract:** `.env.example` keeps `TENSOR_TRADE_WRITE_ENABLED=false`.

---

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G11 | `map-listing.test.ts` missing `resolveOnChainBuyBlockReason` / `tradeListingHasOnChainBuyMetadata` tests | P1 | `tests/map-listing.test.ts` | spawn oc-bg-6 |
| G12 | BuyNowModal honest gate source tests (block reason + partner-primary settlement) | P1 | `tests/trade-modals.test.ts` | spawn oc-bg-7 |
| G13 | trade-panel sweep hint conflates partner vs missing seller enrichment | P1 | `trade-panel.tsx` | spawn oc-bg-8 |
| G14 | Cert-keyed Tensor enrichment merge test (mint from vaultedUrl + listState) | P1 | `tests/partner-listings.test.ts` | spawn oc-bg-9 |
| G15 | Staging pre-flight guardrail (write flag false + enrichment prereq in checklist) | P2 | `tests/*guardrails*` | spawn oc-bg-10 |
| G6 | Broker fee PDA registration | P0 | ops | deferred (operator) |
| G7 | Staging checklist + first fill record | P0 | ops/staging | deferred (operator) |
| G8 | `attachBrokerFeeAccounts` stub (TC-075) | P1 | `tensor-fees.ts` | deferred until broker PDA + IDL |
| G9 | P2-INT-OC08 whitelist mints | P2 | `lib/onchain/collections.ts` | deferred |
| G10 | Full RPC post-fill verify (P2-INT-T16) | P2 | `tensor-fill-verify.ts` | deferred until staging fill |

---

### Spawned workers

| Worker | Gap ID | Task title | Agent ID | Status |
|--------|--------|------------|----------|--------|
| oc-bg-6 | G11 | On-chain — map-listing gate unit tests | `10d5aaa6-2cef-4762-a69f-90316c477b69` | spawned (not awaited) |
| oc-bg-7 | G12 | On-chain — buy modal honest gate tests | `5a8f7c91-5354-4764-97ac-5877f159daee` | spawned (not awaited) |
| oc-bg-8 | G13 | On-chain — sweep enrichment hint copy | `bbc8395e-390a-46fc-bc45-445db4fc4da1` | spawned (not awaited) |
| oc-bg-9 | G14 | On-chain — cert-keyed enrichment test | `6ee50491-4e43-4e82-be66-daaaaa277d79` | spawned (not awaited) |
| oc-bg-10 | G15 | On-chain — staging checklist guardrail | `5a3cb8c6-49df-4efa-abc4-0b3eb9a65fb0` | spawned (not awaited) |

**Spawned this iteration:** 5 / 5 lane budget  
**Global in-flight after spawn:** ~10 code-adjusted (segment 5 + onchain oc-bg-6..10); journal nominal 30 if prior rows not marked complete

---

### Inline fixes (orchestrator)

| File | Change |
|------|--------|
| — | none this tick — prior inv1/2 inline fixes sufficient; worker spawns cover remaining M3 test + copy gaps |

---

## End of invocation 3

**Total iterations (today):** 3  
**Total spawned (invocation 3):** 5  
**Global cap at stop:** ~10/24 code-adjusted in-flight  
**Green criteria met:** partial — BFF + SDK routes green (82 tx/gate tests pass); client mint mapping + grid gates fixed; **M3 test/guardrail coverage in flight**; **staging checklist incomplete**; broker PDA ops pending  
**Recommend next lane:** **qa** (confirm oc-bg-6..10 + re-run focused tx suite) or **backend** (seller enrichment ingest coverage) on next meta tick  
**Operator action needed:** Broker PDA registration, staging write flags, first staging fill dry-run per [trade-staging-checklist.md](../../trade-staging-checklist.md)
