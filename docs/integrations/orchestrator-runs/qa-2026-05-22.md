# QA orchestrator — 2026-05-22

## Invocation 1 — 2026-05-22T17:46:36Z

**Iteration:** 1 of 1  
**Elapsed:** ~17m

### Audit summary

- `npm run test` — 480 tests, **477 pass**, **3 fail** (exit 1, ~828s)
- `npm run build` — skipped (test not green)
- `npm run lint` — skipped (test not green)
- Failure class: **test** — trade landing / featured banner parity (UI lane overlap)
- Global in-flight fix workers (today's journals): **29** (segment 14, ui 5, onchain 5, security 5) — **≥ 24 cap**

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G1 | Toolbar missing `New mints` label (had `NEW MINTS`) | P1 | `components/trade/trade-landing-index-toolbar.tsx` | inline |
| G2 | Landing desk passes `firstLiveCollection?.volume24hSol ?? landingAggregate.volume24hSol`; test expects `volume24hSol={landingAggregate.volume24hSol}` | P1 | `components/trade/trade-landing-desk-client.tsx` | deferred (cap) |
| G3 | Landing desk missing `TRADE_ROUTES.portfolio` reference for sell/portfolio CTA wiring | P1 | `components/trade/trade-landing-desk-client.tsx` | deferred (cap) |

**First 3 failures (detail):**

1. `landing index toolbar exposes Tensor NEW MINTS and 1h chips` — `tests/trade-landing.test.ts` — regex `/New mints/` not matched in toolbar source
2. `M2 landing desk uses single aggregate ribbon without duplicate stats strip` — `tests/trade-landing.test.ts` — regex `/volume24hSol=\{landingAggregate\.volume24hSol\}/` not matched
3. `TradeLandingFeaturedBanner shows aggregate listed and buy/sell stats` — `tests/trade-portfolio.test.ts` — regex `/TRADE_ROUTES\.portfolio/` not matched in desk client

### Spawned workers

| Worker | Gap ID | Task title | Status |
|--------|--------|------------|--------|
| — | — | — | **cap reached** — skipped all spawns |

**Spawned count:** 0 (max 5 this iteration; global cap 29/24)  
**Inline fixes:** G1 — toolbar chip label `NEW MINTS` → `New mints`

### Iteration footer

- **Remaining gaps:** 2 — G2 (aggregate ribbon volume prop), G3 (portfolio route wiring)
- **consecutiveGreenIterations:** 0 (need 2 for green stop)
- **Recommend next lane:** UI lane (landing hero / index parity) or re-run QA after in-flight workers drain
- **Stop reason:** max iterations (1)

---

## End of invocation

**Total iterations:** 1  
**Total spawned:** 0 (cap reached)  
**Green criteria met:** no — 3 test failures (1 inline partial)  
**Operator action needed:** none — wait for global worker drain (29 in-flight) then re-invoke QA lane

---

## Invocation 2 — 2026-05-22T19:26:00Z

**Iteration:** 1 of 1  
**Elapsed:** ~45m

### Audit summary

- `npm run test` — 499 tests, **499 pass**, **0 fail** (exit 0, ~970s)
- `npm run build` — **fail** (exit 1) — TS error fixed inline; subsequent retries blocked by concurrent `next build` / `.next` corruption (5 node.exe processes)
- `npm run lint` — skipped (build not green)
- Failure class: **build** — code (TS re-export scope, fixed) + **flaky/env** (parallel builds racing on `.next`)
- Global in-flight fix workers (today's journals): **24** (segment 9, ui 5, onchain 5, security 5) — **at 24 cap**

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G1 | `getSolanaRpcUrl` not in local scope — re-export only | P1 | `lib/marketplace-config.ts` | inline fixed |
| G2 | Build ENOENT / lock — concurrent `next build` + dev node processes | P2 | `.next/` (env) | deferred — operator: stop parallel builds, re-run build |

**Build errors (detail):**

1. `./lib/marketplace-config.ts:49:13` — `Cannot find name 'getSolanaRpcUrl'` — fixed by adding local import + re-export
2. `ENOENT: pages-manifest.json` / `Another next build process is already running` — environmental; TS phase passed after G1 fix

### Spawned workers

| Worker | Gap ID | Task title | Status |
|--------|--------|------------|--------|
| — | — | — | **cap reached** — skipped all spawns (24/24 in-flight) |

**Spawned count:** 0 (max 5 this iteration; global cap 24/24)  
**Inline fixes:** G1 — `import { getSolanaRpcUrl } from "@/lib/solana-config"` + named re-export in `lib/marketplace-config.ts`

### Iteration footer

- **Remaining gaps:** 1 — G2 (isolated build verify after concurrent workers drain)
- **consecutiveGreenIterations:** 1 (tests 0 failures; need 2 for green stop)
- **Recommend next lane:** QA re-invoke after in-flight workers complete — run isolated `npm run build` then `npm run lint`
- **Stop reason:** max iterations (1)

---

## End of invocation

**Total iterations:** 1  
**Total spawned:** 0 (cap reached)  
**Green criteria met:** partial — tests green (1 consecutive); build/lint not verified  
**Operator action needed:** pause concurrent `npm run build` / dev on host; re-run build to confirm G1 fix

---

## Invocation 3 — 2026-05-22T23:15:00Z

**Iteration:** 1 of 1  
**Elapsed:** ~10m  
**Meta tick:** 4 — verify pass **NOT due** (due at tick 6)

### Audit summary

- `npm run test` — 500 tests, **492 pass**, **8 fail** (exit 1, ~596s)
- `npm run build` — skipped (test not green)
- `npm run lint` — skipped (test not green)
- Failure class: **test** — M2 desk UI parity (label casing, stats ribbon labels, collection tab `soon` vs live wiring)
- Global in-flight fix workers (today's journals): **39** (segment 9, ui 10, backend 5, onchain 5, security 10) — **≥ 24 cap**

### Backlog (P0/P2 — not blocking this audit)

| ID | Item | Status |
|----|------|--------|
| P0-TST-05 | E2E purchase smoke on deploy | TODO — no Playwright |
| P0-TST-06 | Route-level `POST /api/admin/pricing` test | TODO |
| P0-OPS-06 | `qa:ci` on every PR | TODO — CI wiring |
| P2-TST-02 | Discover page smoke (flag on) | TODO |
| P2-TST-03 | Sync external listings integration test | TODO |
| P2-TST-04 | Proxy/middleware integration test | TODO |

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G1 | Instant sell tile — tests expect `Sell now` / `All bids`; source has `SELL NOW` / `ALL BIDS` | P1 | `components/trade/trade-instant-sell-tile.tsx` | deferred (cap) — UI lane ui-bg-3 |
| G2 | Stats ribbon — test expects `24h sales`; source has `24H SALES` | P1 | `components/trade/tensor/stats-grid.tsx` | deferred (cap) — UI/segment |
| G3 | Collection desk ORDERS/TRAITS/HODLERS — P0 tests want live tabs (no `soon: true`); M2 tests want `soon: true` + panel wiring | P1 | `components/trade/trade-collection-desk-client.tsx` | deferred (cap) — UI lane ui-bg-8 |
| G4 | Featured banner — tests expect `Buy now` / `Sell now`; source has `BUY NOW` / `SELL NOW` | P1 | `components/trade/trade-landing-featured-banner.tsx` | deferred (cap) — UI lane |
| G5 | Build/lint not verified this tick | P2 | — | deferred — re-run after tests green |

**First 3 failures (detail):**

1. `TradeInstantSellTile exposes sell-now and all-bids CTAs` — `tests/listing-grid.test.ts` — `/Sell now/` not matched (`SELL NOW` in source)
2. `M2 collection desk tabs: BIDS live, ORDERS/TRAITS/HODLERS panels wired` — `tests/trade-collection-desk.test.ts` — tab/panel wiring regex mismatch during run
3. `M2 instant sell first grid cell` — `tests/trade-desk-p0.test.ts` — `/Sell now/` not matched

**All 8 failures:**

| # | Test | File |
|---|------|------|
| 1 | TradeInstantSellTile exposes sell-now and all-bids CTAs | `tests/listing-grid.test.ts` |
| 2 | M2 collection desk tabs: BIDS live, ORDERS/TRAITS/HODLERS panels wired | `tests/trade-collection-desk.test.ts` |
| 3 | M2 instant sell first grid cell | `tests/trade-desk-p0.test.ts` |
| 4 | M2 stats ribbon: buy/sell tint + 7 metric cells | `tests/trade-desk-p0.test.ts` |
| 5 | P0 collection desk TRAITS tab: trait distribution from filtered listings | `tests/trade-desk-p0.test.ts` |
| 6 | P0 collection desk ORDERS tab: wallet-gated orders table shell | `tests/trade-desk-p0.test.ts` |
| 7 | P0 collection desk HODLERS tab: wallet-gated holder distribution shell | `tests/trade-desk-p0.test.ts` |
| 8 | TradeLandingFeaturedBanner shows featured collection hero and buy/sell stats | `tests/trade-portfolio.test.ts` |

### Spawned workers

| Worker | Gap ID | Task title | Status |
|--------|--------|------------|--------|
| — | — | — | **cap reached** — skipped all spawns (39/24 in-flight) |

**Spawned count:** 0 (max 5 this iteration; global cap 39/24)  
**Inline fixes:** none — label/tab gaps overlap in-flight UI workers (ui-bg-3, ui-bg-8, ui-bg-9)

### Iteration footer

- **Remaining gaps:** 8 test failures + build/lint unverified + P0/P2 backlog (6 items)
- **consecutiveGreenIterations:** 0 (reset from 1 — tests regressed)
- **Recommend next lane:** UI lane (M2 label casing + collection tab live/soon reconciliation); re-run QA after worker drain
- **Stop reason:** max iterations (1)

---

## End of invocation

**Total iterations:** 1  
**Total spawned:** 0 (cap reached)  
**Green criteria met:** no — 8 test failures; build/lint skipped  
**Operator action needed:** wait for global worker drain; verify pass scheduled meta tick 6 (not tick 4)

---

## Invocation 4 — 2026-05-23T00:05:00Z

**Iteration:** 1 of 1  
**Elapsed:** ~25m  
**Meta tick:** 5 — verify pass **NOT due** (due at tick 6)

### Audit summary

- `npm run test` — 530 tests, **529 pass**, **1 fail** (exit 1, ~628s)
- `npm run build` — skipped (test not green on primary audit)
- `npm run lint` — skipped (test not green on primary audit)
- Failure class: **test** — landing desk featured banner wiring (source regex parity)
- Post-inline re-run — 552 tests, **548 pass**, **4 fail** (exit 1, ~1186s); +22 tests from in-flight worker diffs; FeaturedBanner **green** after inline fix
- Global in-flight fix workers (today's journals): **24/24** — segment bg-1..9 + seg-bg-15..19 (14), backend bg-11..15 (5), ui ui-bg-11..15 (5) — **cap reached**

### Backlog (P0/P2 — not blocking this audit)

| ID | Item | Status |
|----|------|--------|
| P0-TST-05 | E2E purchase smoke on deploy | TODO — no Playwright |
| P0-TST-06 | Route-level `POST /api/admin/pricing` test | TODO — auth test exists; route-level gap remains |
| P0-OPS-06 | `qa:ci` on every PR | TODO — `.github/workflows/ci.yml` exists; confirm gate enforced |
| P2-TST-02 | Discover page smoke (flag on) | TODO |
| P2-TST-03 | Sync external listings integration test | TODO |
| P2-TST-04 | Proxy/middleware integration test | TODO |

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G1 | Landing desk missing `firstLiveCollection?.href` / `?.name` in featured banner wiring | P1 | `components/trade/trade-landing-desk-client.tsx` | **inline fixed** |
| G2 | Stats ribbon test expects `Listed`; UI lane changed label to `LISTED` | P1 | `components/trade/tensor/stats-grid.tsx` | deferred — UI lane U39 inline |
| G3 | `getAdminOpsStatus` DB upsert leg returns `warn` not `pass` | P1 | `lib/admin-ops-status.ts`, `lib/data-sync.ts` | deferred — backend bg-7 in flight |
| G4 | `loadAllTradeListings` integration tests throw `methodName must be a method` | P1 | `lib/trade/all-listings.ts`, `tests/trade-all-listings.test.ts` | deferred — backend bg-11..15 + segment seg-bg-15..19 |
| G5 | Build/lint not verified this tick | P2 | — | deferred — re-run after tests green |

**Primary audit failure (detail):**

1. `TradeLandingFeaturedBanner shows featured collection hero and buy/sell stats` — `tests/trade-portfolio.test.ts` — regex `/firstLiveCollection\?\.href/` not matched in desk client (used `liveCollections[0]?.href` instead)

**Post-inline re-run failures (4 — concurrent worker churn):**

| # | Test | File | Likely owner |
|---|------|------|--------------|
| 1 | M2 stats ribbon: stats grid renders all metric labels | `__tests__/trade-stats-ribbon-toolbar.test.ts` | UI (LISTED label) |
| 2 | getAdminOpsStatus passes when DATABASE_URL set and last db upsert succeeded | `tests/admin-ops-observability.test.ts` | backend |
| 3 | loadAllTradeListings merges CC, Phygitals, and treasury… | `tests/trade-all-listings.test.ts` | backend/segment |
| 4 | loadAllTradeListings surfaces fromFallback… | `tests/trade-all-listings.test.ts` | backend/segment |

### Spawned workers

| Worker | Gap ID | Task title | Status |
|--------|--------|------------|--------|
| — | — | — | **cap reached** — skipped all spawns (24/24 in-flight) |

**Spawned count:** 0 (max 5 this iteration; global cap 24/24)  
**Inline fixes:** G1 — moved `firstLiveCollection` before featured vars; wired `firstLiveCollection?.href` + `firstLiveCollection?.name` in `trade-landing-desk-client.tsx`

### Iteration footer

- **Remaining gaps:** 4 test failures on re-run (worker mid-edit) + build/lint unverified + P0/P2 backlog (6 items)
- **consecutiveGreenIterations:** 0 (primary audit 1 fail; re-run 4 fail — reset)
- **Recommend next lane:** **meta tick 6 verify pass** after global worker drain; **backend** (loadAllTradeListings + admin ops) + **UI** (Listed/LISTED test alignment)
- **Stop reason:** max iterations (1)

---

## End of invocation

**Total iterations:** 1  
**Total spawned:** 0 (cap reached)  
**Green criteria met:** no — primary 1 fail (inline fixed); re-run 4 fail from concurrent workers  
**Operator action needed:** wait for bg-11..15 + seg-bg-15..19 + ui-bg-11..15 to land; run verify pass at meta tick 6
