# Security orchestrator — 2026-05-24

## Invocation 1 — 2026-05-24T19:30:00Z (meta tick 5)

**Lane agent ID:** `0908d6f4-d187-48ee-8434-8b5e45e68592`  
**Iteration:** 1 of 1  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path

### Branch / tree note

Working tree on `cursor/plan-build-verify` was missing the GRAILS security + trade-tx stack on disk (marketing-only checkout). Restored security surfaces from `cursor/restore-full-nav-marketplace` via:

`git checkout cursor/restore-full-nav-marketplace --` (csrf, wallet-challenge, tensor-tx-bff, `app/api/trade/tx/*`, guardrails tests, orchestrator docs, `SECURITY_CHECKLIST.md`, `package.json` scripts).

### Audit summary

| Check | Result |
|-------|--------|
| `npm run test:guardrails` | **99/99 pass**, 0 fail (~259s) |
| `npm run ops:env` | **3 ERROR** (placeholder `NEXTAUTH_SECRET`, `ADMIN_PASSWORD`, `CRON_SECRET` — local dev expected); **2 WARN** (`SYNC_API_TOKEN`, P0-SEC-09 unset in dev) |
| `npm run ops:env:production` | See footer below |
| `/api/trade/tx/*` guards | **buy, list, bid, delist, cancel-bid** — `requireTrustedTensorTxOrigin` + `assertTensorTradeWriteEnabled` + `requireTradeTxWalletChallenge` (when flag on); **challenge** — origin gate + `issueTradeTxChallenge` |
| CSRF on tx routes | **N/A (GET-only)** — mirrors legacy checkout: origin gate + optional wallet challenge + proxy rate limits (not `requireCsrfProtection`) |
| `SECURITY_CHECKLIST.md` | Reconciled — GRAILS tx section green; last-reviewed date → 2026-05-24 |

### Trade tx guard pattern (parity with marketplace reserve)

1. **Origin gate** — `TENSOR_TX_REQUIRE_TRUSTED_ORIGIN=true` → `requireTrustedTensorTxOrigin` (`lib/csrf.ts` trusted origins).
2. **Write gate** — `TENSOR_TRADE_WRITE_ENABLED` (503 when off; M3 honest staging).
3. **Wallet challenge** — `TRADE_TX_REQUIRE_WALLET_CHALLENGE=true` → query `challengeId` + `walletSignature` from `GET /api/trade/tx/challenge` (same shape as reserve POST body fields).

### Gaps (top 3 for meta)

| # | Gap | Severity | Notes |
|---|-----|----------|-------|
| G1 | Production env not armed for trade writes | P0 / ops | Enable `TRADE_TX_REQUIRE_WALLET_CHALLENGE` + `TENSOR_TX_REQUIRE_TRUSTED_ORIGIN` when `TENSOR_TRADE_WRITE_ENABLED=true`; wire Upstash `KV_REST_*` for durable `/api/trade/tx/*` rate limits (checklist § GRAILS) |
| G2 | Shared `ADMIN_PASSWORD` admin model | P0 | P0-SEC-11 deferred — bearer JWT RBAC on admin routes is in place; per-user accounts still open |
| G3 | Branch drift / partial checkout risk | P1 | Security stack absent on `cursor/plan-build-verify` until restore-branch checkout; merge or cherry-pick security paths before deploy |

### Spawned workers

**Spawn count:** **0** (guardrails green after restore; no new guardrail failures to assign; global cap 24 — headroom preserved)

| Worker | Gap | Status |
|--------|-----|--------|
| — | — | none |

### Inline fixes

- Restored trade-tx routes + BFF + guardrail tests from `cursor/restore-full-nav-marketplace`
- `SECURITY_CHECKLIST.md` — last-reviewed stamp 2026-05-24

### Iteration footer

- **Stop reason:** 1 iteration complete; guardrails **0 fail ×1** (need ×2 consecutive meta ticks for lane green stop)
- **Recommend next lane:** **security** (re-run after operator sets prod env flags) or **onchain** (client challenge wiring + staging fill rehearsal)
- **Global in-flight fix workers:** 0/24 (this lane)

### `npm run ops:env:production`

- **3 ERROR:** `NEXT_PUBLIC_SITE_URL` missing; `CRON_SECRET` missing/placeholder
- **4 WARN:** `RESERVE_REQUIRE_WALLET_CHALLENGE`, `TRADE_TX_REQUIRE_WALLET_CHALLENGE`, Upstash KV, `SYNC_API_TOKEN` fallback
- **Failed** (expected on local dev without production `.env`)
