# Backend orchestrator — 2026-05-24

## Invocation 1 — 2026-05-24T19:10:00Z (meta tick #5 resume)

**Agent ID:** `7db00140-70af-48c1-a6f5-a35e492e6f7e`  
**Iteration:** 1 of 1  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path  
**Priority alignment:** (1) DB+batch sync + preflight, (2) API-first CC ingest, (3) `/trade/all` aggregation, Tensor optional only

### Workspace blocker

On-disk tree at `c:\Users\amars\Desktop\SVF` is **marketing-only** (`package.json` has `dev` / `build` / `start` / `lint` only). Missing on disk: `prisma/`, `tests/`, `scripts/orchestrator/`, `.cursor/skills/`, `lib/partner-listings.ts`, `lib/trade/all-listings.ts`, ingest/sync scripts. Cursor glob index still lists full GRAILS stack (prior branch).

**Cannot execute:** `npm run db:preflight:warn`, `npm run sync:discover`, backend test bundle, or code edits without full checkout restore.

**Explore subagent (read-only):** `1afca71d-f56a-41e4-ad49-a7bb4041688b`

### Audit summary (indexed / 2026-05-23 baseline)

| Focus area | Status |
|------------|--------|
| `ExternalListing` upsert | **Implemented** in full tree (`lib/external-listings.ts`, `lib/external-listings-sync.ts`); live upsert **ops-blocked** when `DATABASE_URL` protocol incompatible with Prisma proxy (`lib/db-connection.ts`) |
| `db:preflight:warn` runbook | **Implemented** in full tree; **not runnable** on current checkout |
| CC API primary ingest | **Implemented** — `fetchPartnerApiRows` / `COLLECTOR_CRYPT_MARKETPLACE_API_AVAILABLE`; scrape via `shouldUseLiveScrapeFallback` + flags only |
| Phygitals honest stub | **Implemented** — `PHYGITALS_LIVE_INGEST_AVAILABLE=false` → seed/DB only (`lib/phygitals-listings.ts`) |
| `/trade/all` dedupe + venue tags | **Implemented** — `dedupePartnerTradeListings`, `alternateVenueAsks`, `loadAllTradeListings()` |
| Tensor API | **Optional enrichment only** — `enrichPartnerListingsWithTensor` gated on `isTensorReadConfigured()` |

**Tests (last full-tree run, 2026-05-23):** `tests/partner-listings.test.ts` **28/28 pass**. Not re-run this invocation.

### Inline fixes

**0** — no backend source files on disk.

### Fix workers spawned

**Spawn count: 0** (workspace sparse; spawning leaf fix workers would fail immediately — operator must restore full GRAILS branch before backend lane workers).

| Planned worker (deferred) | Gap | Would target |
|---------------------------|-----|----------------|
| be-bg-24-1 | API-before-scrape assertion | `tests/partner-listings.test.ts` |
| be-bg-24-2 | Phygitals partial preserve on CC failure | `tests/external-listings-sync.test.ts` |
| be-bg-24-3 | `ccIngestSource` admin ops exposure | `lib/data-sync.ts`, `tests/data-sync-external-listings.test.ts` |

### Top data gaps (P1–P3)

1. **G-OPS-UPSERT** — Live Postgres `ExternalListing` upsert often no-op until operator fixes `DATABASE_URL` + runs `npm run db:preflight` (`lib/external-listings.ts`, `lib/db-connection.ts`).
2. **G-PHY-LIVE** — Phygitals live ingest off (`PHYGITALS_LIVE_INGEST_AVAILABLE=false`); no API branch in `fetchPartnerApiRows` (`lib/phygitals-listings.ts`).
3. **G-AGG-PRICE** — Cross-venue best-price graph / landing ribbon stats still incomplete without Tensor (`lib/trade/all-listings.ts`, `lib/trade-landing.ts`, `lib/trade/tensor-ribbon-metrics.ts`).

### Secondary gaps (logged, not top 3)

- CC API production confidence depends on env + endpoint discovery (`lib/collector-crypt-api-listings.ts`).
- Deep links / BFF `vaultedUrl` — prior tick spawned be-bg-1..5; verify on full tree.
- Checkout restore: `package.json` scripts `sync:discover`, `db:preflight`, `db:preflight:warn`.

### Global concurrency

- **This lane spawned:** 0  
- **Global cap:** 24 (unchanged)  
- **Meta tick:** #5 — verify pass due cumulative tick #6

### Next operator action

Restore full GRAILS working tree (same branch as 2026-05-23 backend pass), then re-run backend lane or spawn deferred be-bg-24-* workers.

---

## Invocation 2 — 2026-05-24T22:05:00Z (meta tick #6 immediate)

**Agent ID:** `43607468-8d6f-4034-8175-245887cfded3` (backend lane; subagent tick)  
**Iteration:** 1 of 1  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path  
**Priority alignment:** (1) DB+batch sync + preflight, (2) API-first CC ingest, (3) `/trade/all` aggregation, Tensor optional only

### Workspace status

Full GRAILS tree **present on disk** (restored vs invocation 1 sparse-checkout report). `package.json` includes `sync:discover`, `db:preflight`, `test`, partner ingest libs, Prisma schema.

### Audit summary

| Focus area | Status |
|------------|--------|
| `ExternalListing` upsert | **Implemented** — `upsertExternalListings()` in `lib/external-listings.ts`; batch via `syncExternalListingsToJson()` |
| Live upsert ops-blocked | **Active blocker** — `npm run db:preflight:warn` → severity `unreachable`, Prisma proxy URL; hint: run `npx prisma dev` or switch to direct `postgresql://` |
| `db:preflight:warn` runbook | **Runnable** — preflight step added to `docs/runbooks/operations-hardening.md` (inline this tick) |
| CC API primary ingest | **Implemented** — `fetchCollectorCryptIngestListings()` API-first; batch tests green; request-path API-before-scrape test **gap** |
| Phygitals honest stub | **Implemented** — `PHYGITALS_LIVE_INGEST_AVAILABLE=false`; seed refresh via `refreshPhygitalsSeedRows()`; admin ops visibility **gap** |
| `/trade/all` dedupe + venue tags | **Implemented** — `dedupePartnerTradeListings`, `alternateVenueAsks`, BFF deep-link tests present |
| Tensor API | **Optional enrichment only** — landing/partner paths tested with `TENSOR_API_KEY` unset; ingest-derived `sellNowSol` on preview cards **partial gap** |
| Cron `POST /api/sync` | **Wired** — `syncAllData()` → `syncExternalListingsToJson()`; preflight hint in operatorHints when zero upsert **gap** |
| Silent upsert skip | **Gap** — `dbConfigured=true` + `dbUpsertCount=0` when protocol/write skip does not always surface in `errors[]` |

**Tests (this invocation):**

- `tests/partner-listings.test.ts` + `external-listings-sync.test.ts` + `external-listings.test.ts` + `data-sync-external-listings.test.ts` — **65/65 pass** (~212s)
- `npm run db:preflight:warn` — **warn-only exit 0**; scenario `unreachable` (Prisma proxy fetch failed)

### Inline fixes

**1** — `docs/runbooks/operations-hardening.md`: preflight step before `sync:discover`; CC **API-first** copy (was scrape-only); Phygitals `PHYGITALS_LIVE_INGEST_AVAILABLE=false` note.

### Fix workers spawned (not awaited)

**Spawn count: 5** | **Global in-flight (estimated):** 15 / 24 | **Remaining budget:** 9  
*(segment tick #5: 5 + onchain tick #6: 5 + backend: 5)*

| Worker ID | Title | Gap | Target |
|-----------|-------|-----|--------|
| `122350c5-7631-4366-8d4a-c03283b7e48a` | Backend — sync upsert skip errors | G-UPSERT-SILENT | **landed** (usage limit before test finish) — `blockedReason` + sync `errors[]` |
| `65375f71-a85e-4d18-a35f-7421476b8266` | Backend — sync preflight operatorHints | G-PREFLIGHT-HINT | **landed** — `buildExternalListingsDbOperatorHints` + tests |
| `c9d8502d-bac4-4b66-804d-53fe0a7e9b7a` | Backend — landing sellNow without Tensor | G-AGG-PRICE | **no-op** — `mapCollectionPreview` already sets `sellNowSol` from partner floor |
| `d6ad8ce3-276a-4d71-8bf9-5872d94b339c` | Backend — CC API request-path test | G-CC-API-READ | **landed** — `listPartnerTradeListings prefers CC API and skips live scrape` |
| `a11f3ead-bd0a-4381-b29f-ff92d6ee9cd1` | Backend — Phygitals stub ops visibility | G-PHY-OPS | **landed** — `formatPhygitalsIngestModeLabel` + admin-ops tests |

### Top ingest/upsert gaps (remaining)

1. **G-OPS-UPSERT (P0)** — Live Postgres upsert blocked: `DATABASE_URL` Prisma proxy unreachable locally; operator must `npx prisma dev` or direct `postgresql://` + `npm run db:preflight` before prod cron upsert.
2. **G-PHY-LIVE (P2, expected)** — Phygitals live API unavailable; seed-only honest stub remains correct until partner API ships.
3. **G-OPS-UPSERT (P0)** — Operator still must fix `DATABASE_URL` / run `npx prisma dev` for live upsert (preflight `unreachable` in orchestrator shell).

### Secondary gaps (logged)

- CC stable marketplace API production confidence (P2-INT-CC04–07) — API candidates wired; partner endpoint env-dependent.
- Cross-venue best-price graph without Tensor partially addressed by landing ribbon worker.
- Invocation 1 deferred be-bg-24-* items largely **landed in tree** from 2026-05-23 workers (API-first batch tests, ccIngestSource metadata, BFF deep links).

### Global concurrency

- **This lane spawned:** 5  
- **Global in-flight (estimated):** 15 / 24  
- **Cap status:** **open** (9 slots remaining)  
- **Meta tick:** #6 — verify pass due (running in parallel)

### Iteration footer

- **Spawned:** 5 fix workers  
- **Remaining gaps:** G-OPS-UPSERT (operator); code gaps G-UPSERT-SILENT / G-PREFLIGHT-HINT / G-CC-API-READ / G-PHY-OPS **closed in tree**; workers hit usage limits but diffs landed  
- **Recommend next lane:** **qa** (verify pass results + ingest test bundle after workers land)  
- **Stop reason:** LANE_ITERATIONS=1 complete; do not await workers

---

## End of invocation 2

**Total iterations:** 1  
**Total spawned this invocation:** 5 (agent IDs above, not awaited)  
**Green criteria met:** partial — backend bundle 65/65 pass; live DB upsert ops-blocked; 2 consecutive green meta ticks not yet met  
**Operator action needed:**
- Fix `DATABASE_URL` protocol / start `npx prisma dev` for local upsert verification
- Run `npm run db:preflight` on production (P0-DAT-04 / P0-OPS-09)
- Review worker diffs when complete; no commit unless approved

**Status:** active — inv2 tick complete (5 workers spawned).
