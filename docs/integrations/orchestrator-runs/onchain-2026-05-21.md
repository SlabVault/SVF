# On-chain orchestrator — 2026-05-21

## Invocation 1 — 2026-05-22T12:15:00Z

**Iteration:** 1 of 5  
**Elapsed:** 0m

### Audit summary

- BFF tx routes present: `buy`, `list`, `delist`, `bid`, `cancel-bid` under `app/api/trade/tx/`
- SDK: `lib/onchain/clients/tensor-tcm.ts`, `tensor-tcm-sdk.ts`, `tensor-tx-bff.ts`; buy supports `writePath=sdk|rest`; list/delist REST-only
- Write gates: `TENSOR_TRADE_WRITE_ENABLED` / `NEXT_PUBLIC_*` in `lib/trade/trade-modal.ts`, trade panel, modals
- Hooks: `use-tensor-buy|list|delist|bid|cancel-bid` exist; **BuyNowModal** confirm not wired to buy hook
- CSRF: none on tx routes — **security lane** follow-up required
- Broker PDA: ops pending per `docs/trade-staging-checklist.md` — **operator blocker**, no auto-fix
- Tx tests: `tests/api-trade-tx.test.ts` + `tests/tensor-tcm.test.ts` — **36 pass / 0 fail** (focused run)

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G1 | BuyNowModal confirm does not invoke on-chain buy | P0 | `buy-now-modal.tsx` | spawn |
| G2 | List route lacks SDK `writePath=sdk` | P0 | `list/route.ts`, `tensor-tcm.ts` | spawn |
| G3 | `use-tensor-buy` omits `writePath=sdk` | P1 | `use-tensor-buy.ts` | spawn |
| G4 | No `resolveListWritePath` in BFF | P1 | `tensor-tx-bff.ts` | spawn |
| G5 | Delist route REST-only | P1 | `delist/route.ts` | spawn |
| G6 | Item detail / nft-card buy path incomplete | P1 | `trade-item-detail-client.tsx` | spawn |
| G7 | PlaceOffer may use wrong mint field | P1 | `place-offer-modal.tsx` | spawn |
| G8 | CSRF missing on tx routes | P0 | `app/api/trade/tx/*` | deferred → security lane |
| G9 | Broker PDA not registered on-chain | P0 | ops | deferred (operator) |
| G10 | Staging checklist unchecked (M3) | P0 | `trade-staging-checklist.md` | spawn docs |
| G11 | List SDK test coverage | P2 | `api-trade-tx.test.ts` | spawn |

### Spawned workers

| Worker | Gap ID | Task title | Status |
|--------|--------|------------|--------|
| bg-1 | G1 | On-chain buy-now modal wire | spawned (not awaited) |
| bg-2 | G2 | On-chain list SDK route | spawned (not awaited) |
| bg-3 | G3 | On-chain buy writePath=sdk | spawned (not awaited) |
| bg-4 | G4 | On-chain list writePath helper | spawned (not awaited) |
| bg-5 | G5 | On-chain delist SDK route | spawned (not awaited) |
| bg-6 | G6 | On-chain item detail buy | spawned (not awaited) |
| bg-7 | G7 | On-chain place-offer mint fix | spawned (not awaited) |
| bg-8 | G8 | On-chain CSRF handoff doc | spawned (not awaited) |
| bg-9 | G9 | On-chain broker ops log | spawned (not awaited) |
| bg-10 | G11 | On-chain list SDK tests | spawned (not awaited) |

**Spawned count:** 10 (max 10 this iteration)  
**Inline fixes:** `.env.example` already documents staging-only write flags + broker PDA gate

### Iteration footer

- **Remaining gaps:** 9 functional + staging checklist + broker ops
- **Recommend next lane:** continue on-chain (iteration 2); security lane for CSRF after functional tx paths land
- **Stop reason:** continuing — staging checklist incomplete; tx tests green (1/2 consecutive)

---

## Invocation 1 — 2026-05-22T12:16:00Z

**Iteration:** 2 of 5  
**Elapsed:** ~4m

### Audit summary

- Re-ran focused tx tests: **36 pass / 0 fail** (2nd consecutive green for tx suite)
- Segment M3 acceptance: pre-flight checklist still all `[ ]`; no verified mainnet fill logged
- Tensor trade panel sweep/delist tabs present; multi-listing sweep may lack batch buy error surfacing

### Gaps (iteration 2 focus)

| ID | Gap | Severity | Action |
|----|-----|----------|--------|
| G12 | Sweep panel batch buy error UX | P1 | spawn |
| G13 | `use-tensor-tx` unified error messages | P2 | spawn |
| G14 | Portfolio inventory delist CTA wiring | P1 | spawn |
| G15 | `buildDelistTransaction` SDK stub if missing | P2 | spawn |
| G16 | Staging runbook tx signature log template | P2 | spawn |
| G17 | `use-tensor-list` pass writePath=sdk | P1 | spawn |
| G18 | Fee split preview in ops/admin surface | P2 | spawn |
| G19 | cancel-bid REST vs SDK parity check | P2 | spawn |
| G20 | Trade modals test: buy handler assertion | P2 | spawn |

### Spawned workers

| Worker | Gap ID | Task title | Status |
|--------|--------|------------|--------|
| bg-11 | G12 | On-chain sweep batch errors | spawned (not awaited) |
| bg-12 | G13 | On-chain tensor-tx errors | spawned (not awaited) |
| bg-13 | G14 | On-chain portfolio delist | spawned (not awaited) |
| bg-14 | G15 | On-chain delist SDK builder | spawned (not awaited) |
| bg-15 | G16 | On-chain staging runbook template | spawned (not awaited) |
| bg-16 | G17 | On-chain list hook writePath | spawned (not awaited) |
| bg-17 | G18 | On-chain fee preview surface | spawned (not awaited) |
| bg-18 | G19 | On-chain cancel-bid parity | spawned (not awaited) |
| bg-19 | G20 | On-chain trade-modals buy test | spawned (not awaited) |

**Spawned count:** 9  
**Inline fixes:** none

### Iteration footer

- **Remaining gaps:** M3 staging verification (operator), broker PDA, CSRF (security)
- **Recommend next lane:** security lane (CSRF on tx routes) in parallel with on-chain iter 3
- **Stop reason:** continuing — checklist incomplete despite tx tests green ×2

---

## Invocation 1 — 2026-05-22T12:17:00Z

**Iteration:** 3 of 5  
**Elapsed:** ~5m

### Audit summary

- Confirmed prod contract: `TENSOR_TRADE_WRITE_ENABLED` default false in `.env.example`
- `getSvfBrokerPubkey()` reads treasury from `data/site.json` — on-chain registration unverified
- No partner ingest files touched this lane

### Gaps (iteration 3 focus)

| ID | Gap | Severity | Action |
|----|-----|----------|--------|
| G21 | Enrichment propagates `listState` to TradeListing consistently | P1 | spawn |
| G22 | Bid route `makerBroker` default uses SVF pubkey — document in staging | P2 | spawn |
| G23 | Trade instant-sell modal on-chain path | P2 | spawn |
| G24 | `tensor-tcm-sdk` takerBroker on fill | P1 | spawn |
| G25 | api-trade-tx bid/cancel-bid negative tests | P2 | spawn |

### Spawned workers

| Worker | Gap ID | Task title | Status |
|--------|--------|------------|--------|
| bg-20 | G21 | On-chain listState enrichment | spawned (not awaited) |
| bg-21 | G22 | On-chain bid broker docs | spawned (not awaited) |
| bg-22 | G23 | On-chain instant-sell wire | spawned (not awaited) |
| bg-23 | G24 | On-chain takerBroker fill | spawned (not awaited) |
| bg-24 | G25 | On-chain bid route tests | spawned (not awaited) |

**Spawned count:** 5  
**Inline fixes:** none

### Iteration footer

- **Remaining gaps:** operator staging fill + broker PDA
- **Recommend next lane:** on-chain iter 4 (hardening) + operator runbook
- **Stop reason:** continuing

---

## Invocation 1 — 2026-05-22T12:18:00Z

**Iteration:** 4 of 5  
**Elapsed:** ~6m

### Audit summary

- Tx route handlers remain GET (CSRF exposure until security lane adds POST + challenge)
- `slabvault-broker` program stub — devnet deploy not in scope

### Gaps (iteration 4 focus)

| ID | Gap | Severity | Action |
|----|-----|----------|--------|
| G26 | Segment 4 status doc sync in grails-step-by-step-plan | P2 | spawn |
| G27 | onchain-trade-stack M3 status line update | P2 | spawn |
| G28 | Trade panel sell DELIST disabled tooltip when no listing | P2 | spawn |
| G29 | Helius + SDK list price validation | P2 | spawn |
| G30 | `previewFeeSplit` link from staging checklist | P2 | **done** — `trade-staging-checklist.md` engineering preview + `onchain-trade-stack.md` fees.ts |

### Spawned workers

| Worker | Gap ID | Task title | Status |
|--------|--------|------------|--------|
| bg-25 | G26 | On-chain M3 plan sync | spawned (not awaited) |
| bg-26 | G27 | On-chain stack doc status | spawned (not awaited) |
| bg-27 | G28 | On-chain delist gate tooltip | spawned (not awaited) |
| bg-28 | G29 | On-chain list price validation | spawned (not awaited) |
| bg-29 | G30 | On-chain fee preview link | spawned (not awaited) |

**Spawned count:** 5  
**Inline fixes:** none

### Iteration footer

- **Remaining gaps:** manual staging steps 1–5, broker PDA confirmation
- **Recommend next lane:** operator staging verification; security CSRF
- **Stop reason:** continuing to iteration 5

---

## Invocation 1 — 2026-05-22T12:19:00Z

**Iteration:** 5 of 5  
**Elapsed:** ~7m

### Audit summary

- Final tx test snapshot: **36 pass / 0 fail** (api-trade-tx + tensor-tcm)
- Staging checklist: **not complete** — all pre-flight and verification items open
- Broker PDA: **operator action required** — no spawn for mainnet registration

### Gaps (deferred / handoff)

| ID | Gap | Action |
|----|-----|--------|
| G9 | Broker PDA registration on Tensor Fees | **operator** — fund wallet, register treasury, record tx |
| G8 | CSRF on `/api/trade/tx/*` | **security lane** |
| G1–G30 | Functional fixes | **30 background workers spawned** (iterations 1–5); not awaited |

### Spawned workers

| Worker | Gap ID | Task title | Status |
|--------|--------|------------|--------|
| bg-30 | G9 | On-chain operator handoff summary | spawned (not awaited) |

**Spawned count:** 1  
**Inline fixes:** none

### Iteration footer

- **Remaining gaps:** staging checklist (all sections), broker PDA ops, CSRF, worker merge review
- **Recommend next lane:** **operator** staging fill → **security** CSRF → **QA** full `npm test`
- **Stop reason:** max iterations (5); broker PDA blocker logged; tx tests green ×3 but checklist incomplete

---

## End of invocation

**Total iterations:** 5  
**Total spawned:** 30 (background Task workers, not awaited)  
**Green criteria met:** tx route tests green for 3 consecutive audits; **staging checklist not complete**  
**Operator action needed:** see [Operator handoff checklist](#operator-handoff-checklist) below (broker PDA, staging env, no auto-deploy).

## Status: active

Remaining P0: broker PDA ops, staging manual verification, CSRF (security lane). Recommend security + operator staging before enabling prod writes.

---

## Operator handoff checklist

**Scope:** Manual ops only. Orchestrator iteration 5 (G9) deferred broker registration to operators. **Agents, CI, and background workers must not** submit Fees-program registration txs, deploy `slabvault-broker` or other SVF programs to mainnet, flip production write flags, or register the broker PDA from this repo.

**Canonical references:** [trade-staging-checklist.md](../../trade-staging-checklist.md) (pre-flight, env table, verification steps 1–5), [onchain-trade-stack.md](../onchain-trade-stack.md) (Fees program ID `TFEEgwDP6nn1s8mMX2tTNPPz8j2VomkphLUmyxKm17A`, PDA table).

### Hard stops (engineering + automation)

- [ ] **No mainnet auto-deploy** — Do not deploy Anchor programs, run `anchor deploy` against mainnet-beta, or enable prod `TENSOR_TRADE_WRITE_ENABLED` / `NEXT_PUBLIC_TENSOR_TRADE_WRITE_ENABLED` from orchestrator spawns.
- [ ] **No broker PDA registration from repo** — Registration is operator-blocked; log blockers in ops runbook, do not workaround via CI secrets or scripted txs.
- [ ] **Production write gates stay off** — `.env.example` defaults `TENSOR_TRADE_WRITE_ENABLED=false`; prod remains read path + partner ingest until partner sign-off and checklist complete.

### Broker fee PDA (operator — Tensor Fees)

Fees program: `TFEEgwDP6nn1s8mMX2tTNPPz8j2VomkphLUmyxKm17A`. Treasury recipient (from `data/site.json`): `2oRZe7z9Jx3rpoUWuidjGhpX9mxxhtps2JqQtdxLfHwg`.

| Step | Action | Done |
|------|--------|------|
| 1 | Confirm treasury pubkey is the intended **broker fee recipient** (or document approved hot wallet). | [ ] |
| 2 | Fund operator wallet (or arrange partner co-sign for collection-owned fee configs). | [ ] |
| 3 | Register SVF as broker on Tensor Fees per [tensor-foundation/fees](https://github.com/tensor-foundation/fees) admin flow (global and/or per-collection). | [ ] |
| 4 | For CC / Phygitals slugs in `TENSOR_CC_COLLECTION_SLUGS`, confirm partner approval before attaching collection-scoped broker fees. | [ ] |
| 5 | Verify broker / collection fee accounts exist for SVF pubkey (explorer, eigen CLI, or Fees client); **record PDA address(es)** in staging run log. | [ ] |
| 6 | Align staging `SVF_BROKER_PUBKEY` with on-chain recipient if set; leave unset to default to treasury. | [ ] |
| 7 | Dry-run `/api/trade/tx/buy` on staging — simulation must not fail with missing broker / `maker broker not yet enabled`. | [ ] |
| 8 | (Optional) After first staging fill, confirm broker lamports to treasury; attach tx signature to run log. | [ ] |

If step 5–7 fail: **keep write flags off**, document blocker (partner co-sign, whitelist, pNFT rules); engineering does not auto-register PDAs.

### Staging environment (operator — before first fill)

Set on **staging deployment only** (not production):

| Variable | Staging value | Notes |
|----------|---------------|-------|
| `TENSOR_TRADE_WRITE_ENABLED` | `true` | Gates `/api/trade/tx/*` builders |
| `NEXT_PUBLIC_TENSOR_TRADE_WRITE_ENABLED` | `true` | Enables buy/list in trade panel and modals |
| `TENSOR_API_KEY` | Set | Tensor REST + tx routes |
| `TENSOR_CC_COLLECTION_SLUGS` | e.g. `collector_crypt,phygitals` | CC / Phygitals slug mapping |
| `SOLANA_RPC_URL` | Mainnet-beta RPC | Server simulation + blockhash |
| `NEXT_PUBLIC_SOLANA_RPC` | Reachable from browser | Wallet connect |
| `HELIUS_API_KEY` | Recommended | DAS cert↔mint fallback |
| `DATABASE_URL` | Postgres | `ExternalListing` partner cache |
| `SVF_BROKER_PUBKEY` | Optional | Only if recipient ≠ treasury display |
| `SVF_BROKER_FEE_BPS` | Optional | **Preview/UI only** — bps live on-chain in Fees state |

Also verify pre-flight in [trade-staging-checklist.md](../../trade-staging-checklist.md#pre-flight): `/trade` loads, partner ingest + Tensor enrichment, staging wallet funded for fees.

**Production:** leave `TENSOR_TRADE_WRITE_ENABLED=false` and `NEXT_PUBLIC_TENSOR_TRADE_WRITE_ENABLED=false` until M3 sign-off.

### Staging verification (operator — after env + PDA)

Run in order (details in [trade-staging-checklist.md — Staging verification steps](../../trade-staging-checklist.md#staging-verification-steps)):

1. [ ] Read path — `/trade/c/collector-crypt` listings + enrichment on item detail.
2. [ ] Enrichment — partner list shows `tensorEnrichment.enrichedSeller > 0` with API key + slugs.
3. [ ] Buy dry-run — wallet connected; Buy returns serialized tx from `/api/trade/tx/buy` (wallet signs; does not auto-send from server).
4. [ ] Write gate — with write flags `false`, buy UI disabled / fallback message.
5. [ ] Footer ticker — aggregate vol when `TENSOR_API_KEY` configured.

Record first successful fill tx signature in the staging run log when ready.

### Parallel lanes (not operator PDA work)

| Lane | Item | Owner |
|------|------|-------|
| Security | CSRF / POST + challenge on `/api/trade/tx/*` (G8) | security orchestrator |
| Engineering | Functional tx/modal fixes (G1–G30 workers) | on-chain workers — merge review |
| QA | Full `npm test` after merges | QA orchestrator |

### Handoff sign-off

- [ ] Broker PDA verified on-chain (step 5 above) — **not** done by agents in this repo.
- [ ] Staging env applied; production write flags confirmed `false`.
- [ ] Staging verification steps 1–5 checked or blockers logged.
- [ ] CSRF security lane tracked before prod write enablement.
