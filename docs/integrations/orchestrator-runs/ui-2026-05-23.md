# UI / Tensor parity orchestrator — 2026-05-23

## Invocation 1 — 2026-05-23T00:00:00Z

**Lane:** ui  
**Iteration:** 1 of 1  
**Milestone:** M2 desk UX shell parity on real M1 data  
**Priority alignment:** P3 aggregation hardening · P4 deep links on every partner row  
**Global in-flight fix workers (before spawn):** ~6 lane orchestrators active (meta tick); worker cap headroom assumed available

---

### Audit summary

Browser MCP crawl (cursor-ide-browser) + code audit vs GRAILS `/trade`:

| URL | Regions captured |
|-----|------------------|
| [tensor.trade/](https://www.tensor.trade/) | Nav, Tensorians hero, CARDS\|TABLE toggle, filter bar, index loading state |
| [tensor.trade/trade/solana_monkey_business](https://www.tensor.trade/trade/solana_monkey_business) | Trade panel BUY/SELL, ITEMS/INFO/BIDS/ORDERS/TRAITS/HODLERS tabs, grid toolbar, instant sell, grid BUY buttons |
| [tensor.trade/item/5fF5PBH…](https://www.tensor.trade/item/5fF5PBHauH2JbPnJ6xHxPtRpMJcpbVMxnvwvkiPTj8mp) | Item page title + commerce shell (slow load) |
| `http://localhost:3000/trade/all` | **BLOCKED** — Next.js build error `UnhandledSchemeError: node:fs` in client bundle; route audit deferred to code review |

Copy checklist parity **~67%** unchanged (15 done · 14 partial · 2 missing · 2 screenshot). This tick focuses **P3/P4** gaps: aggregate desk UX, landing stats from merged listings, partner deep-link CTAs on grid/item/modal.

**Tensor reference (collection desk):** Every grid tile exposes actionable BUY; GRAILS disables Buy when seller/listState missing even when `vaultedUrl` exists — violates P4 “collector can buy somewhere today.”

**GRAILS code findings:**

- `deskListings` prop on `TradeLandingDeskClient` aliased `_deskListings` — unused; market pulse sums preview rows not deduped merge
- `TensorNftCard` sets `buyDisabled` when `missingOnChainSellerMeta` — no partner ↗ fallback
- `trade-item-detail-client.tsx` — BUY NOW wallet-gated with no partner primary CTA
- `BuyNowModal` — partner link secondary below disabled Confirm buy
- `TensorTradePanel` sweep throws on partner-only rows on aggregate desk

---

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| U1 | Grid tile Buy disabled without seller meta despite `vaultedUrl` | P4 | `tensor/nft-card.tsx` | spawn ui-bg-1 |
| U2 | Item commerce stack lacks partner deep link primary CTA | P4 | `trade-item-detail-client.tsx` | spawn ui-bg-2 |
| U3 | BuyNowModal partner checkout demoted vs disabled on-chain confirm | P4 | `buy-now-modal.tsx` | spawn ui-bg-3 |
| U4 | Landing market pulse uses preview sum; `deskListings` unused | P3 | `trade-landing-desk-client.tsx`, `app/trade/page.tsx` | spawn ui-bg-4 |
| U5 | Sweep panel not honest on aggregate/partner-only listings | P3 | `tensor/trade-panel.tsx` | spawn ui-bg-5 |
| U6 | Local `/trade/all` browser verify | P1 | build graph | **blocked** — node:fs client import (backend/segment) |
| U7 | Stats ribbon vol/sales/Δ unkeyed | P2 | `tensor/stats-grid.tsx` | deferred — ingest/backend |
| U8 | Portfolio inventory UI | screenshot | `portfolio/*` | blocked G41 |

---

### Spawned workers

| Worker | Gap ID | Task title | Agent ID | Status |
|--------|--------|------------|----------|--------|
| ui-bg-1 | U1 | UI parity — nft-card partner buy CTA | b7a28670-ce99-4e57-a59e-8c514f18da0b | spawned (not awaited) |
| ui-bg-2 | U2 | UI parity — item detail partner CTA | e6cb663c-2863-49fe-8642-1660eac53eb2 | spawned (not awaited) |
| ui-bg-3 | U3 | UI parity — buy modal partner primary | 086f519f-a25e-4025-b50b-fd1515abf7a1 | spawned (not awaited) |
| ui-bg-4 | U4 | UI parity — landing merged aggregate stats | 013e08c2-5eb6-4498-9dc0-43992fe325b4 | spawned (not awaited) |
| ui-bg-5 | U5 | UI parity — trade panel partner sweep honesty | 52371d4b-a478-48a7-848b-51e17577ed39 | spawned (not awaited) |

**Spawned count:** 5 (max 5 this iteration)

**Inline fixes:**

- `trade-collection-desk-client.tsx` — aggregate desk status banner copy emphasizes “Buy on partner ↗ when on-site TCM fill is unavailable”

---

### Iteration footer

- **Remaining gaps:** U6 build blocker (node:fs); U7 ingest-backed ribbon metrics; U8 portfolio screenshot gate; P0 checklist not green
- **Recommend next lane:** **backend** — resolve client `node:fs` import chain so `/trade/all` browser verify unblocks; **qa** after ui-bg-1..5 land
- **Stop reason:** max iterations (1/1)

---

## End of invocation

**Total iterations:** 1  
**Total spawned:** 5 (ui-bg-1..ui-bg-5, not awaited)  
**Green criteria met:** no — P4 deep links incomplete until workers land; local build blocked  
**Operator action:** Review ui-bg-1..5 diffs; no commit unless approved. Start `npm run dev:clean` after backend fixes node:fs graph.

---

## Invocation 2 — 2026-05-23T12:00:00Z

**Lane:** ui  
**Iteration:** 1 of 1 (meta re-invoke)  
**Milestone:** M2 desk UX shell · tick focus P3/P4 deep links + `/trade/all` on real aggregated data  
**Global in-flight fix workers (before spawn):** **25/24** — segment seg-bg-20..24 (5), onchain oc-bg-1..5 (5), backend be-bg-1..5 (5), ui ui-bg-1..5 (5), security sec-bg-1..5 (5) all `spawned (not awaited)` → **`cap reached (concurrency cap)`**

---

### Audit summary

Browser MCP crawl (cursor-ide-browser) + live GRAILS verify on `localhost:3002`:

| URL | Regions captured |
|-----|------------------|
| [tensor.trade/](https://www.tensor.trade/) | Nav, Tensorians hero, CARDS\|TABLE toggle, filter bar, loading table state |
| [tensor.trade/trade/solana_monkey_business](https://www.tensor.trade/trade/solana_monkey_business) | Trade panel BUY/SELL, ITEMS/INFO/BIDS/ORDERS/TRAITS/HODLERS tabs, grid toolbar, instant sell |
| `http://localhost:3002/trade/all` | **PASS** — 9 merged listings, all grid tiles show **Buy ↗**, aggregate banner, 7-cell stats ribbon, venue filter rail, activity PREVIEW column |
| `http://localhost:3002/trade/c/collector-crypt` | CC partner desk loads with **Buy ↗** on partner rows |

Copy checklist parity **~70%** (P4 grid/item deep links landed in code; partial rows unchanged). Prior invocation U1–U5 fixes **verified in browser** on aggregate desk — every listed row actionable via partner ↗.

**Code re-audit vs invocation 1 gaps:**

- `tensor/nft-card.tsx` — `showPartnerBuyLink` + primary **Buy ↗** `<a>` when off-chain ✅
- `trade-item-detail-client.tsx` — `partnerPrimaryCheckout` + **Open on partner ↗** ✅
- `buy-now-modal.tsx` — `isPartnerPrimaryCheckout` still gated on `vaultedUrl` only → **inline fix** (see below)
- `trade-landing-desk-client.tsx` — `landingAggregateOverride` wired ✅
- `tensor/trade-panel.tsx` — aggregate/partner sweep honesty (ui-bg-5 pending worker)

---

### Gaps

| ID | Gap | Severity | Action |
|----|-----|----------|--------|
| U6 | `/trade/all` browser verify | P1 | **closed** — loads on :3002 with 9 rows + Buy ↗ |
| U7 | Stats ribbon 24H SALES / live Δ | P2 | deferred — ingest/Tensor statsV2 |
| U8 | Portfolio inventory UI | screenshot | blocked G41 |
| U9 | Activity rows link to item, not partner ↗ | P3 | acceptable parity (Tensor links to item/modal) |
| U10 | Buy modal partner-primary when cert-resolved URL (no vaultedUrl) | P4 | **inline** — `buy-now-modal.tsx` |
| U11 | ORDERS / TRAITS / HODLERS tabs | P2 | `CollectionTabSoon` stubs |
| U12 | ⌘K full mint resolver | P2 | deferred M5 |

---

### Spawned workers

**`cap reached (concurrency cap)`** — 25 in-flight repo-wide; **0 spawned** this iteration.

---

### Inline fixes

- `buy-now-modal.tsx` — `isPartnerPrimaryCheckout` uses `tradeListingUsesPartnerSiteSettlement` / `tradeListingShowsPartnerBuyLink` instead of requiring `vaultedUrl` (aggregate + CC cert-search deep links)

---

### Iteration footer

- **Remaining gaps (top 3):** U7 ingest-backed ribbon vol/sales/Δ; U8 portfolio wallet screenshot gate; U11 ORDERS/TRAITS/HODLERS tab shells
- **Recommend next lane:** **qa** — verify ui-bg-1..5 + inline modal fix; **backend** if CC desk shows `marketplace-slabs` DB error overlay in dev
- **Stop reason:** max iterations (1/1) + global cap reached

---

## End of invocation 2

**Total iterations:** 1  
**Total spawned:** 0 (`cap reached`)  
**Green criteria met:** no — P0 checklist not green; portfolio + live activity still partial  
**Operator action:** Review inline `buy-now-modal.tsx` diff; no commit unless approved.

---

## Invocation 3 — 2026-05-23T12:15:00Z (meta tick 4)

**Lane:** ui  
**Iteration:** 1 of 1 (meta tick 4)  
**Milestone:** M2 desk UX shell · tick focus P3/P4 deep links + `/trade/all` on real aggregated data  
**Global in-flight fix workers (before spawn):** **15/24** — segment seg-bg-25..29 (5), backend be-bg-6..10 (5), onchain oc-bg-6..10 (5); inv1–2 ui/onchain/backend workers **landed in tree** (599 pass / 0 fail); **headroom 9**

---

### Audit summary

Browser MCP crawl (cursor-ide-browser) + live GRAILS verify on `localhost:3002`:

| URL | Regions captured |
|-----|------------------|
| [tensor.trade/](https://www.tensor.trade/) | Nav, Tensorians hero, CARDS\|TABLE toggle, filter bar |
| [tensor.trade/trade/solana_monkey_business](https://www.tensor.trade/trade/solana_monkey_business) | Trade panel BUY/SELL, ITEMS/INFO/BIDS/ORDERS/TRAITS/HODLERS tabs, grid toolbar |
| `http://localhost:3002/trade/all` | **PASS** — 9 merged listings, all grid tiles **Buy ↗**, aggregate banner, 7-cell stats ribbon, venue filter rail, activity PREVIEW (venue badges, no partner ↗ on rows) |

Copy checklist parity **~72%** (grid/item/modal deep links done; activity + offer modal + alternate-venue grid hints partial).

**Prior tick fixes verified in browser:** Every listed row on `/trade/all` actionable via **Buy ↗**; aggregate desk banner + sweep honesty copy present.

**Remaining P3/P4 gaps (this tick):**

- Activity feed rows link to GRAILS item only — no partner ↗ on partner-only events
- `PlaceOfferModal` dead-end when no on-chain mint (no partner checkout)
- Grid ignores `alternateVenueAsks` cheaper-venue hint on multi-venue certs
- Landing `TradeLandingMarketPulse` not linked to `/trade/all`
- Grid **Bid** button misleading/disabled vs partner-only rows (no honest fallback)

---

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| U6 | `/trade/all` browser verify | P1 | — | **closed** — 9 rows + Buy ↗ |
| U13 | Activity feed partner ↗ on aggregate desk | P4 | `trade-activity-feed.tsx`, `trade-activity-panel.tsx` | spawn ui-bg-6 |
| U14 | PlaceOfferModal partner checkout fallback | P4 | `place-offer-modal.tsx` | spawn ui-bg-7 |
| U15 | Grid alternateVenueAsks cheaper-venue chip | P3 | `tensor/nft-card.tsx` | spawn ui-bg-8 |
| U16 | Landing market pulse → `/trade/all` CTA | P3 | `trade-landing-market-pulse.tsx` | spawn ui-bg-9 |
| U17 | Grid Bid honest partner-only fallback | P4 | `tensor/nft-card.tsx` | spawn ui-bg-10 |
| U7 | Stats ribbon 24H SALES / live Δ | P2 | `tensor/stats-grid.tsx` | deferred — ingest |
| U8 | Portfolio inventory UI | screenshot | `portfolio/*` | blocked G41 |
| U11 | ORDERS / TRAITS / HODLERS tabs | P2 | `CollectionTabSoon` stubs | deferred |
| U12 | ⌘K full mint resolver | P2 | `trade-command-palette.tsx` | deferred M5 |

---

### Spawned workers

| Worker | Gap ID | Task title | Agent ID | Status |
|--------|--------|------------|----------|--------|
| ui-bg-6 | U13 | UI parity — activity feed partner ↗ | dcc59781-fbe6-451d-909a-62cdc3d74078 | spawned (not awaited) |
| ui-bg-7 | U14 | UI parity — offer modal partner CTA | d4cdfaca-a1a1-48a7-b94c-80b61a8cb74f | spawned (not awaited) |
| ui-bg-8 | U15 | UI parity — grid alternate venue chip | 8a017c21-17f3-4cc8-bbf0-50c057d17c8a | spawned (not awaited) |
| ui-bg-9 | U16 | UI parity — landing pulse all listings link | a2ec50bc-5903-43e2-a396-067f92622279 | spawned (not awaited) |
| ui-bg-10 | U17 | UI parity — grid bid partner fallback | fef0bf4d-16ca-4fd0-82d3-4b6b3d11c27c | spawned (not awaited) |

**Spawned count:** 5 (max 5 this iteration; global after spawn **~20/24**)

**Inline fixes:** none

---

### Iteration footer

- **Remaining gaps (top 3):** U7 ingest-backed ribbon vol/sales/Δ; U8 portfolio wallet screenshot gate; U11 ORDERS/TRAITS/HODLERS tab shells
- **Recommend next lane:** **qa** — verify ui-bg-6..10 when landed; **backend** for BFF aggregate stats (be-bg-6..10)
- **Stop reason:** max iterations (1/1)

---

## End of invocation 3

**Total iterations:** 1  
**Total spawned:** 5 (ui-bg-6..10, not awaited)  
**Green criteria met:** no — P4 deep links incomplete until workers land; P0 checklist not green  
**Operator action:** Review ui-bg-6..10 diffs; no commit unless approved.
