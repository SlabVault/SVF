# On-chain orchestrator — 2026-05-24

## Invocation 1 — 2026-05-24T19:18:00Z (meta tick #5 resume)

**Agent ID:** `96086f51-2194-4519-96c0-8768f1d84cc7` (on-chain lane; subagent tick)  
**Iteration:** 1 of 1  
**North star:** M3 staging prep — seller enrichment, staging checklist alignment, honest buy gates when writes off  
**Scope:** `app/api/trade/tx/*`, `lib/onchain/*` — **no ingest edits**; prod `TENSOR_TRADE_WRITE_ENABLED` stays **false**

### Workspace blocker

On-disk tree at `c:\Users\amars\Desktop\SVF` is **marketing-only** (`package.json`: `dev` / `build` / `start` / `lint` only). Missing on disk: `app/api/trade/`, `lib/onchain/` (except not present), `scripts/orchestrator/run-onchain-lane.md`, `.cursor/skills/grails-onchain-orchestrator/`, `docs/trade-staging-checklist.md`, `docs/integrations/m3-recovery-status-2026-05-22.md`, `tests/trade-staging-guardrails.test.ts`.

Cursor index still exposes full GRAILS on-chain stack (readable in IDE): `lib/onchain/tensor-tx-bff.ts`, `app/api/trade/tx/buy|list|delist/route.ts`, `lib/integrations/tensor.ts`, `lib/onchain/index.ts`.

**Cannot execute:** `run-onchain-lane.md` script, staging guardrail tests, broker PDA CLI, or route edits that persist without full checkout restore.

### Audit summary (indexed code + meta P5 focus)

| Focus area | Status |
|------------|--------|
| **Honest buy gates** (`TENSOR_TRADE_WRITE_ENABLED` off) | **Partial** — `TensorTradeWriteDisabledError` message exists (`lib/onchain/tensor-tx-bff.ts`); all write routes call `assertTensorTradeWriteEnabled()` **before** query validation (`app/api/trade/tx/buy/route.ts` L38). Response is generic `{ error }` 503 — no `code`, `tradeWriteEnabled`, or `recoveryHint` for UI/deep-link hierarchy |
| **Seller enrichment (M3 fill)** | **Gap** — SDK buy requires `owner` or `listState` (`buy/route.ts` L65–69); partner aggregate path must enrich seller wallet / list state before staging fill — not verified on sparse tree (`lib/trade/resolve-listing.ts` indexed, not on disk) |
| **`trade-staging-checklist.md` / m3-recovery alignment** | **Not on disk** — docs absent in checkout; segment lane owns cross-doc sync per meta P6 |
| **Broker fee PDA / first staging fill** | **Ops-blocked (log only)** — exports: `findBrokerConfigPda`, `BROKER_CONFIG_SEED`, `getSvfBrokerPubkey`, `isSvfBrokerConfigured` (`lib/onchain/index.ts`); operator must init fee PDA + record first fill per `docs/integrations/staging-first-fill-record-template.md` (indexed, not on disk). **No auto-fix this tick** |
| **Tx routes** | **Indexed** — `buy`, `list`, `delist`, `bid`, `cancel-bid`, `challenge` under `app/api/trade/tx/`; shared BFF: RPC gate, optional origin + wallet challenge, REST vs SDK `writePath` |
| **Prod write flag** | **Correct default** — `isTensorTradeWriteEnabled()` → `parseEnvFlag(TENSOR_TRADE_WRITE_ENABLED, false)` (`lib/integrations/tensor.ts`) |

### Operator blockers (broker PDA / staging fill — LOG ONLY)

1. Confirm `SLABVAULT_BROKER_PROGRAM_ID` / broker pubkey env per `lib/onchain/program-ids.ts` (indexed).
2. Derive and init **broker config PDA** (`findBrokerConfigPda` / `BROKER_CONFIG_SEED`) on staging cluster before first list/fill.
3. Set staging-only `TENSOR_TRADE_WRITE_ENABLED=true` only after RPC + Tensor key + broker PDA checklist complete.
4. File first fill in staging fill record template when operator executes fill (not automated).

### Inline fixes

**0** — no on-chain source files on disk.

### Fix workers spawned

**Spawn count: 0** (workspace sparse; spawning leaf fix workers would fail immediately — restore full GRAILS branch first).

| Planned worker (deferred) | Gap | Would target |
|---------------------------|-----|----------------|
| oc-bg-24-1 | Structured write-disabled JSON on buy/list/delist | `lib/onchain/tensor-tx-bff.ts`, `app/api/trade/tx/*/route.ts` |
| oc-bg-24-2 | Client honest buy gate + `tradeWriteEnabled` probe | `components/trade/buy-now-modal.tsx`, `lib/wallet-transaction-access-messages.ts` |
| oc-bg-24-3 | Seller `owner` / `listState` enrichment for Tensor SDK path | `lib/trade/resolve-listing.ts`, item detail buy CTA |
| oc-bg-24-4 | Align `trade-staging-checklist.md` ↔ m3-recovery-status | `docs/trade-staging-checklist.md`, `docs/integrations/m3-recovery-status-2026-05-22.md` |
| oc-bg-24-5 | Staging guardrails test for write-off buy response | `tests/trade-staging-guardrails.test.ts` |

### Top 3 on-chain gaps

1. **G-WRITE-GATE** — Write routes hard-503 via `assertTensorTradeWriteEnabled()` with no machine-readable `tradeWriteEnabled: false`; UI cannot honestly explain “staging writes off” vs misconfigured RPC/Tensor key (`tensor-tx-bff.ts`, `app/api/trade/tx/buy/route.ts`).
2. **G-SELLER-ENRICH** — M3 SDK fill blocked without `owner` or `listState`; partner listing rows need seller-wallet enrichment before staging buy path is callable (`buy/route.ts`, `lib/trade/resolve-listing.ts`).
3. **G-BROKER-PDA** — Broker fee PDA + first staging fill remain **operator-owned**; env/PDA init not runnable on current checkout (`lib/onchain/pdas.ts`, `lib/onchain/clients/slabvault-broker.ts`).

### Secondary gaps (logged)

- `bid` / `cancel-bid` routes not audited line-by-line this tick (same BFF gate pattern expected).
- `TENSOR_TX_REQUIRE_TRUSTED_ORIGIN` / `TRADE_TX_REQUIRE_WALLET_CHALLENGE` staging matrix not on disk.
- Full-tree `npm test` / `trade-staging-guardrails.test.ts` not runnable on marketing checkout.

### Global concurrency

- **This lane spawned:** 0  
- **Global cap:** 24 (unchanged)  
- **Meta tick:** #5 — verify pass due cumulative tick #6

### Next operator action

Restore full GRAILS working tree (same branch as 2026-05-23 on-chain pass), then re-run on-chain lane or spawn deferred `oc-bg-24-*` workers (max 5, 1 iter).

---

## Invocation 2 — 2026-05-24T22:00:00Z (meta tick #6 immediate)

**Agent ID:** `f437af15-2dc5-4ef9-aa0e-7b781f58cd5e` (on-chain lane; subagent tick)  
**Iteration:** 1 of 1  
**North star:** M3 staging buy prep — seller enrichment, staging checklist gaps, honest buy gates (`canBuyOnChain`, write flags off in prod), `buildFillTransaction` path docs  
**Scope:** `app/api/trade/tx/*`, `lib/onchain/*`, trade buy UI — **no ingest edits**; prod `TENSOR_TRADE_WRITE_ENABLED` stays **false**

### Workspace status

Full GRAILS tree **present on disk** (restored vs invocation 1). Audited routes, SDK clients, staging checklist, and guardrail tests.

### Audit summary (M3 staging buy prep)

| Focus area | Status |
|------------|--------|
| **Honest buy gates** (`canBuyOnChain` + write flags off) | **Partial** — Client gates correctly require `writeEnabled` + `seller`/`listState` + on-chain settlement (`use-tensor-buy.ts` L56–68). UI tooltips via `tradeWriteDisabledTooltip()` / `resolveOnChainBuyBlockReason()`. **Gap:** BFF write-off returns plain `{ error }` 503 — no `code` / `tradeWriteEnabled` for fetch layer to distinguish expected prod-off vs misconfig |
| **Seller enrichment (M3 fill)** | **Wired, ops-dependent** — `mapTradeListingToTensorNft` maps `sellerWallet`→`listing.seller`, `listState`→`listing.listState` (`map-listing.ts` L54–58). Partner merge + Tensor enrichment in `lib/partner-listings.ts`. Fill blocked without either field at BFF L65–69 and UI gate |
| **`trade-staging-checklist.md` alignment** | **Present** — M3 prep section covers aggregate ingest, seller metadata, `canBuyOnChain` table, broker PDA ops-blocked checklist |
| **`buildFillTransaction` path** | **Done (gated)** — `GET /api/trade/tx/buy?writePath=sdk` → `buildFillTransaction()` (`tensor-tcm.ts` L245–258); `takerBroker` via `getSvfBrokerPubkey()`. Docs in `onchain-trade-stack.md` § Aggregate ingest → buy-path enrichment |
| **Broker fee PDA / first staging fill** | **Ops-blocked (log only)** — `findBrokerConfigPda`, `previewFeeSplit()`, `getSvfBrokerPubkey()` wired; operator must register on Tensor Fees + record first fill. **No auto-fix** |
| **Tx routes** | **Indexed + on disk** — `buy`, `list`, `delist`, `bid`, `cancel-bid`, `challenge`; shared BFF: origin gate → `assertTensorTradeWriteEnabled()` → SDK/REST |
| **Prod write flag** | **Correct** — `isTensorTradeWriteEnabled()` defaults `false` (`lib/integrations/tensor.ts` L89–90); `.env.example` guarded by `trade-staging-guardrails.test.ts` |

### Operator blockers (broker PDA / staging fill — LOG ONLY)

1. Register SVF broker on Tensor Fees program (`TFEEgwDP6nn1s8mMX2tTNPPz8j2VomkphLUmyxKm17A`) — partner co-sign may be required for CC/Phygitals collections.
2. Derive/init broker config PDA on staging cluster (`findBrokerConfigPda` / `BROKER_CONFIG_SEED` in `lib/onchain/pdas.ts`) before first list/fill.
3. Set `TENSOR_TRADE_WRITE_ENABLED=true` + `NEXT_PUBLIC_TENSOR_TRADE_WRITE_ENABLED=true` on **staging only** after RPC + Tensor key + seller enrichment + broker verification.
4. Execute first staging fill dry-run; record in `docs/integrations/staging-first-fill-record-template.md`.

### Tests (orchestrator audit)

Guardrail bundle started (`trade-staging-guardrails`, `api-trade-tx-guardrails`, `tensor-tcm`) — not awaited (workers run targeted tests).

### Inline fixes

**1** — Sharpened `tradeWriteDisabledTooltip()` copy to state prod-default write-off and point to partner checkout (`lib/trade/trade-modal.ts`).

### Fix workers spawned (not awaited)

**Spawn count: 5** | **Global in-flight:** 5 / 24 | **Remaining budget:** 19

| Worker ID | Title | Gap | Target |
|-----------|-------|-----|--------|
| `6b861657-f767-43bc-877b-41e52dd39c4f` | On-chain write-disabled JSON | Structured `TRADE_WRITE_DISABLED` on tx routes | `tensor-tx-bff.ts`, `app/api/trade/tx/*/route.ts` |
| `494ad0a6-1261-4d58-bfdb-0590764298f1` | Client write gate messages | Map BFF code → honest UI when writes off | `use-tensor-tx.ts`, `buy-now-modal.tsx` |
| `84008b51-8e54-4ab6-88ba-f2df0b55d6ba` | Seller metadata buy wiring | End-to-end seller/listState → `canBuyOnChain` | `map-listing.ts`, `trade-item-detail-client.tsx` |
| `70b4f13c-81b6-4c7d-a5e8-78c5588db2aa` | buildFillTransaction docs | Query param matrix + checklist cross-links | `onchain-trade-stack.md`, `trade-staging-checklist.md` |
| `f9d55476-c367-481b-b019-d48354521954` | Staging guardrails write-off test | Machine-readable write-off shape in tests | `trade-staging-guardrails.test.ts` |

### Top 3 M3 on-chain gaps (remaining)

1. **G-WRITE-GATE** — BFF 503 lacks `code: TRADE_WRITE_DISABLED` + `tradeWriteEnabled: false` (worker `6b861657…` in flight).
2. **G-BROKER-PDA** — Broker fee PDA registration + first staging fill **operator-owned**; bid/buy simulation may fail until Fees program state exists.
3. **G-ENRICH-OPS** — Seller metadata pipeline coded but staging fill requires live `TENSOR_API_KEY` + enriched CC test mint (backend/ops dependency).

### Secondary gaps (logged)

- `attachBrokerFeeAccounts` in `tensor-fees.ts` stubbed until IDL codegen (TC-075).
- Tx tests green for **1** meta tick this lane — need **2 consecutive** green ticks before stop condition.
- Security lane: CSRF N/A on GET tx routes; origin gate optional via `TENSOR_TX_REQUIRE_TRUSTED_ORIGIN`.

### Global concurrency

- **This lane spawned:** 5  
- **Global in-flight (estimated):** 5 / 24  
- **Cap status:** **open** (19 slots remaining)  
- **Meta tick:** #6 — verify pass due (running in parallel)

### Iteration footer

- **Spawned:** 5 fix workers  
- **Remaining gaps:** G-WRITE-GATE (in flight), G-BROKER-PDA (ops), G-ENRICH-OPS (staging env)  
- **Recommend next lane:** **backend** (Tensor seller enrichment live data) or **qa** (verify pass results) after workers land; re-run on-chain tick when structured write gate merges
