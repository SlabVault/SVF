# UI / Tensor parity orchestrator — 2026-05-24

## Invocation 1 — 2026-05-24T19:05:00Z (meta tick #5 resume)

**Agent ID:** `9e027f82-fab3-4ce7-b76d-b544a97d6816`  
**Iteration:** 1 of 1 (`LANE_ITERATIONS=1`)  
**Milestone:** M2 desk UX shell (Segment 3) — **skip M5/M6, no homepage redesign**  
**Tick focus:** Buy CTA + deep-link hierarchy (grails-master-ui); M2 desk parity — collection desk, item page, `/trade/all` tiles — venue badges, lowest-ask winner display  
**North star:** cert → all listings → price signals → buy on best path

**Explore audit subagent:** `3c156fcd-d033-4742-893f-bce34cd27f05` (read-only)

---

### Audit summary

**Code audit** (partial filesystem latency; key trade files confirmed on disk via shell):

| Surface | Venue badge | Buy CTA hierarchy | Lowest-ask winner |
|---------|-------------|-------------------|-------------------|
| `tensor/nft-card.tsx` (grid via `trade-listing-tile` re-export) | `VenueBadge` on card image | Partner **Buy ↗** when `tradeListingShowsPartnerBuyLink`; else on-site **Buy** / disabled + tooltip | Primary `askSol` = merge winner; **no tile hint** for `alternateVenueAsks` |
| `trade-collection-desk-client.tsx` | Aggregate banner copy | Uses `TensorTradePanel` + `TradeListingGrid` | Dedupe in `lib/partner-listings.ts` (`mergeAlternateVenueAsks`) |
| `/trade/all` (`app/trade/all/page.tsx`) | `aggregateDesk` + `venuesCount` prop | Same desk client | `loadAllTradeListings()` merge |
| Item (`trade-item-detail-client.tsx`) | `VenueBadge` + `ItemVenueCompareStrip` on overview when 2+ rows | `partnerPrimaryCheckout` / `showPartnerBuyLink` / on-chain buy | `buildVenueCompareRows` from slab route |
| Landing index cards | `VenueBadge` on `TradeLandingIndexCardTile` | Floor/sell-now display only (no per-slab buy) | N/A |

**Buy hierarchy helpers (verified):** `lib/trade/trade-modal.ts` — `tradeListingShowsPartnerBuyLink`, `resolveTradeListingPartnerCheckoutUrl`, `tradeListingUsesPartnerSiteSettlement`; `tensor/map-listing.ts` — `resolveOnChainBuyBlockReason`.

**2026-05-23 regressions addressed in tree:** `trade-listing-tile.tsx` re-exports `TensorNftCard` (grid CTA parity unified). Aggregate desk status banner documents partner ↗ fallback.

**Browser MCP:** Not run this tick (time budget; code audit + P0 tests prioritized). Tensor reference baseline from `ui-2026-05-23.md` crawl retained.

**Tests:** `npm test -- tests/trade-desk-p0.test.ts tests/trade-venue-compare.test.ts` — **started, not awaited** (slow runner).

---

### Gaps (this tick)

| ID | Gap | Severity | Owner | Action |
|----|-----|----------|-------|--------|
| U24-1 | Grid tiles lack lowest-ask / multi-venue winner affordance when `alternateVenueAsks` present | M2 | `tensor/nft-card.tsx` | spawn ui-bg-24-1 |
| U24-2 | Item **COMPARE** tab still **Soon** while overview strip works when 2+ venues | M2 | `trade-item-detail-client.tsx` | spawn ui-bg-24-2 |
| U24-3 | Mobile sticky bar buy hierarchy may lag item/grid partner-primary pattern | M2 | `trade-mobile-action-bar.tsx` | spawn ui-bg-24-3 |
| U24-4 | Aggregate ribbon `venuesCount` visibility unverified on `/trade/all` | M2 | `collection-stats-ribbon.tsx` | spawn ui-bg-24-4 |
| U24-5 | `BuyNowModal` partner checkout may still be secondary to disabled on-chain confirm | P4 | `buy-now-modal.tsx` | spawn ui-bg-24-5 |
| U24-6 | Local browser verify `/trade/all` + item commerce | P1 | dev build | deferred — qa lane |
| U24-7 | Tensor sweep panel honesty on partner-only aggregate rows | P3 | `tensor/trade-panel.tsx` | deferred (2026-05-23 ui-bg-5) |

---

### Spawned workers

| Worker | Gap | Task | Agent ID | Status |
|--------|-----|------|----------|--------|
| ui-bg-24-1 | U24-1 | Grid lowest-ask / +N venues hint on nft-card | `72dbfb2f-3c5c-4470-a74a-fbe7946ab52c` | spawned (not awaited) |
| ui-bg-24-2 | U24-2 | Wire COMPARE tab to ItemVenueCompareStrip | `affbd0fc-f841-46e3-98e4-7dc4307cd5f5` | spawned (not awaited) |
| ui-bg-24-3 | U24-3 | Mobile action bar buy hierarchy parity | `56453dcb-0f14-400e-906c-5c071e060ad9` | spawned (not awaited) |
| ui-bg-24-4 | U24-4 | Stats ribbon venuesCount cell | `3434ea3b-8a22-4389-bd92-b35f61ef97db` | spawned (not awaited) |
| ui-bg-24-5 | U24-5 | Buy modal partner-primary CTA | `cb68c2a0-340b-4647-b241-bcba2cd37ad9` | spawned (not awaited) |

**Spawned count:** 5 (max 5 this iteration)

**Inline fixes:** 0

**Global cap:** 24 — not reached by this lane spawn batch alone; peer lanes may add workers.

---

### Iteration footer

- **Top 3 UI gaps (operator):**
  1. **Multi-venue price signal on desk tiles** — backend merge sets `alternateVenueAsks` but grid does not surface “best price / +N venues” (U24-1).
  2. **Item compare UX split** — compare data wired on slab route; COMPARE tab still gated **Soon** instead of reusing strip (U24-2).
  3. **Commerce shell consistency** — mobile bar + buy modal may not mirror partner-primary hierarchy already on nft-card/item (U24-3, U24-5).

- **Remaining:** U24-6 browser verify; U24-7 sweep panel; copy checklist P0 green (2 consecutive meta ticks).
- **Recommend next lane:** **qa** — run trade P0 bundle after ui-bg-24-* land; **backend** if `/trade/all` data empty.
- **Stop reason:** 1/1 iteration complete; max spawns used.

---

## End of invocation

**Total iterations:** 1  
**Total spawned:** 5 (ui-bg-24-1..5, not awaited)  
**Green criteria met:** no — M2 parity gaps remain until workers land  
**Operator action:** Review ui-bg-24-* diffs; no commit unless approved.
