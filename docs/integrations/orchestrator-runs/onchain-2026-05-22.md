# On-chain orchestrator — 2026-05-22

## Invocation 1 — 2026-05-22T17:45:00Z

**Iteration:** 1 of 1  
**Elapsed:** 0m  
**Global in-flight fix workers (before spawn):** 19 — segment 9 + ui 5 + security 5 — cap headroom **5/24**

---

### Audit summary

**BFF tx routes** (`app/api/trade/tx/`):

| Route | Origin gate | Write gate | Wallet challenge | SDK writePath | Notes |
|-------|-------------|------------|------------------|---------------|-------|
| `buy` | yes | yes | yes | `resolveBuyWritePath` + SDK fill | Green |
| `list` | yes | yes | yes | `resolveListWritePath` + SDK list | Green |
| `delist` | yes | yes | **no** (security lane bg-1) | `resolveListWritePath` + SDK delist | Functional OK; challenge deferred |
| `bid` | yes | yes | yes (security inline iter 1) | REST-only + `makerBroker` default | Green |
| `cancel-bid` | yes | yes | yes | REST-only | Green |
| `challenge` | n/a | n/a | issues challenges | n/a | Green |

**SDK / BFF libs:**

- `lib/onchain/clients/tensor-tcm.ts` — `buildListTransaction`, `buildFillTransaction`, `buildDelistTransaction` present; write-gated
- `lib/onchain/tensor-tx-bff.ts` — `resolveBuyWritePath`, `resolveListWritePath`, `resolveDelistWritePath`, origin + challenge helpers wired
- `lib/onchain/fees.ts` — `getSvfBrokerPubkey`, `previewFeeSplit`; ops route `/api/ops/fee-preview`

**Client hooks / modals:**

- `use-tensor-buy` — `writePath=sdk` via `buildTensorBuyTxSearchParams`; BuyNowModal confirm wired to `buy()`
- `use-tensor-list` — **missing `writePath=sdk`** (defaults REST when `TENSOR_API_KEY` set)
- `use-tensor-delist` — `writePath=sdk` via `buildTensorDelistTxSearchParams`
- `place-offer-modal` — imports `resolveTradeListingMint` but submits **`listing.id`** as mint (cert/id bug)
- `trade-panel` sweep — batch buy loop with error surface; partial-failure copy could name failing listing

**Write gates:**

- Server: `assertTensorTradeWriteEnabled()` on all tx routes
- Client: `isTradeWriteEnabledClient()` in modals, trade panel, hooks
- `.env.example`: `TENSOR_TRADE_WRITE_ENABLED=false` (prod default)

**Staging checklist** (`docs/trade-staging-checklist.md`):

- Pre-flight: all `[ ]` — partner ingest, enrichment, wallet, staging SOL **operator**
- Broker PDA: all registration steps `[ ]` — **operator-blocked**, no auto-fix spawn
- Staging verification steps 1–6: unchecked — requires staging deploy + manual fill

**Tx tests:**

- Prior on-chain lane (2026-05-21): **36 pass / 0 fail** (api-trade-tx + tensor-tcm focused)
- Verify pass today: **RED** (448/462) — failures mostly UI/segment; on-chain guardrails reported green in security lane (69/69)
- Focused re-run this invocation: hung/slow in shell; defer full tx suite to QA lane

**Broker PDA:** Operator action required per checklist — treasury `2oRZe7z9Jx3rpoUWuidjGhpX9mxxhtps2JqQtdxLfHwg`, Fees program `TFEEgwDP6nn1s8mMX2tTNPPz8j2VomkphLUmyxKm17A`. **No spawn.**

**CSRF:** GET tx routes — origin gate policy (not POST CSRF). Security lane owns wallet challenge on delist/cancel-bid (bg-1..bg-5 in flight). **Deferred, no on-chain respawn.**

---

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G1 | `use-tensor-list` omits `writePath=sdk` — REST default when API key set | P0 | `use-tensor-list.ts` | spawn |
| G2 | `PlaceOfferModal` bids with `listing.id` not on-chain mint | P0 | `place-offer-modal.tsx` | spawn |
| G3 | No `buildTensorListTxSearchParams` helper (buy/delist parity) | P1 | `use-tensor-list.ts`, `tests/api-trade-tx.test.ts` | spawn |
| G4 | Delist route uses `resolveListWritePath` not `resolveDelistWritePath` | P2 | `delist/route.ts` | spawn |
| G5 | Sweep batch buy error lacks listing identity in message | P1 | `trade-panel.tsx` | spawn |
| G6 | Delist route wallet challenge | P1 | `delist/route.ts` | deferred → security bg-1 |
| G7 | Broker PDA registration on Tensor Fees | P0 | ops | deferred (operator) |
| G8 | Staging checklist pre-flight + verification | P0 | ops/staging | deferred (operator) |
| G9 | CSRF N/A on GET tx routes | P2 | docs/security | deferred → security lane |

---

### Spawned workers

| Worker | Gap ID | Task title | Status |
|--------|--------|------------|--------|
| oc-bg-1 | G1 | On-chain — list hook writePath=sdk | spawned (not awaited) |
| oc-bg-2 | G2 | On-chain — place-offer mint fix | spawned (not awaited) |
| oc-bg-3 | G3 | On-chain — buildTensorListTxSearchParams | spawned (not awaited) |
| oc-bg-4 | G4 | On-chain — delist resolveDelistWritePath | spawned (not awaited) |
| oc-bg-5 | G5 | On-chain — sweep batch error UX | spawned (not awaited) |

**Spawned count:** 5 (max 5 this iteration; global cap **24/24** after spawn)  
**Inline fixes:** none (env-gate copy already documented in `.env.example` + staging checklist)

---

### Iteration footer

- **Remaining gaps:** G6–G9 (security/operator), staging manual verification, broker PDA ops
- **Recommend next lane:** **operator** staging env + broker PDA → **security** (wallet challenge workers land) → **QA** full `npm test` after worker batch
- **Stop reason:** 1 iteration complete; broker PDA blocker logged; staging checklist incomplete; tx tests green on prior tick (need 2 consecutive for lane green stop)

---

## End of invocation

**Total iterations:** 1  
**Total spawned:** 5 (background Task workers, not awaited)  
**Global cap at stop:** 24/24 in-flight (segment 9 + ui 5 + security 5 + onchain 5)  
**Green criteria met:** partial — functional tx wiring largely complete; **staging checklist not complete**; broker PDA ops pending  
**Operator action needed:** Broker PDA registration, staging write flags, first mainnet fill dry-run per [trade-staging-checklist.md](../../trade-staging-checklist.md)

---

## Invocation 2 — 2026-05-22T19:10:00Z

**Iteration:** 1 of 1  
**Elapsed:** ~8m  
**Global in-flight fix workers (before spawn):** 24 — segment bg-1..bg-9 (9), ui ui-bg-1..ui-bg-5 (5), security bg-1..bg-5 (5), onchain oc-bg-1..oc-bg-5 (5) — **cap reached (24/24)**

---

### Audit summary

**Prior worker targets (oc-bg-1..oc-bg-5) — merged in tree:**

| Gap | Status | Evidence |
|-----|--------|----------|
| G1 list hook `writePath=sdk` | **fixed** | `use-tensor-list.ts` → `buildTensorListTxSearchParams` always sets `writePath: "sdk"` |
| G2 place-offer mint | **fixed** | `place-offer-modal.tsx` uses `resolveTradeListingMint(listing)` |
| G3 list search params helper | **fixed** | `buildTensorListTxSearchParams` + test in `api-trade-tx.test.ts` |
| G4 delist write path resolver | **fixed** | `delist/route.ts` uses `resolveDelistWritePath` |
| G5 sweep batch error UX | **fixed** | `trade-panel.tsx` throws `Failed on "{label}": {message}` |

**BFF tx routes** — unchanged green from invocation 1; delist wallet challenge still deferred to security bg-1.

**Fees on fill (M3 focus):**

- `tensor-tcm-sdk.ts` — `buildSwapFillTransaction` passes `takerBroker` from `getSvfBrokerPubkey()` (falls back to buyer when unconfigured)
- `tensor-fees.ts` — `attachBrokerFeeAccounts` **stub** (TC-075); SDK CPI may include fees metas; explicit attach deferred until broker PDA + IDL codegen
- `GET /api/ops/fee-preview` — admin read-only `previewFeeSplit()` surface live
- Bid route — `makerBroker` defaults to `getSvfBrokerPubkey()` (test covered)

**Broker PDA docs:**

- `trade-staging-checklist.md` — ops checklist present; **added** missing [Engineering preview (`lib/onchain/fees.ts`)](#engineering-preview-libonchainfeests) section (inline, invocation 2)
- `onchain-trade-stack.md` — write-path table notes `takerBroker` on fill (inline)
- Operator registration steps remain `[ ]` — **no auto-fix spawn**

**Staging checklist:** pre-flight + verification all `[ ]`; first fill record template open — **operator-blocked**.

**Tx tests (this invocation):**

- `npx tsx --test tests/tensor-tcm.test.ts tests/api-trade-tx.test.ts` — **48 pass / 0 fail**

**Prod contract:** `.env.example` keeps `TENSOR_TRADE_WRITE_ENABLED=false`.

---

### Gaps

| ID | Gap | Severity | Owner | Action |
|----|-----|----------|-------|--------|
| G6 | Delist route wallet challenge | P1 | security bg-1 | deferred |
| G7 | Broker PDA registration on Tensor Fees | P0 | ops | deferred (operator) |
| G8 | Staging checklist pre-flight + first fill | P0 | ops/staging | deferred (operator) |
| G9 | `attachBrokerFeeAccounts` stub (TC-075) | P1 | eng | deferred until broker PDA + fees IDL |
| G10 | CSRF N/A on GET tx routes | P2 | security | deferred |

---

### Spawned workers

**cap reached (concurrency cap)** — global in-flight 24/24; **0 spawns** this iteration.

Prior on-chain workers (oc-bg-1..oc-bg-5) appear merged; no respawn.

---

### Inline fixes

1. `docs/trade-staging-checklist.md` — Engineering preview section for `fees.ts` / fee-preview route / fill `takerBroker` note
2. `docs/integrations/onchain-trade-stack.md` — `buildFillTransaction` row documents `takerBroker`

---

### Iteration footer

- **Spawned count:** 0 (cap reached)
- **Remaining gaps:** G6–G10; staging manual verification; broker PDA ops; TC-075 fees attach
- **Recommend next lane:** **operator** broker PDA + staging env → **security** (delist challenge) → **QA** verify pass after global cap clears
- **Stop reason:** 1 iteration complete; global cap 24/24; broker PDA operator-blocker; tx tests green this tick (2nd consecutive green for on-chain focused suite)

---

## End of invocation 2

**Total iterations (today):** 2  
**Total spawned (invocation 2):** 0  
**Global cap at stop:** 24/24 in-flight  
**Green criteria met:** partial — tx wiring + focused tests green ×2; **staging checklist incomplete**; broker PDA ops pending  
**Operator action needed:** Broker PDA registration, staging write flags, first mainnet fill dry-run per [trade-staging-checklist.md](../../trade-staging-checklist.md)

## Invocation 3 — 2026-05-22T20:30:00Z

**Iteration:** 1 of 1  
**Elapsed:** ~12m  
**Global in-flight fix workers (before spawn):** 24 — segment bg-1..9 (9), ui ui-bg-6..10 (5), onchain oc-bg-1..5 (5), security sec-bg-1..5 (5) — **cap reached (24/24)**

---

### Audit summary

**Prior on-chain workers (oc-bg-1..oc-bg-5) — confirmed merged:**

| Gap | Status | Evidence |
|-----|--------|----------|
| G1 list hook `writePath=sdk` | **fixed** | `use-tensor-list.ts` → `buildTensorListTxSearchParams` |
| G2 place-offer mint | **fixed** | `place-offer-modal.tsx` → `resolveTradeListingMint` |
| G3 list search params helper | **fixed** | `buildTensorListTxSearchParams` + test |
| G4 delist write path resolver | **fixed** | `delist/route.ts` → `resolveDelistWritePath` |
| G5 sweep batch error UX | **fixed** | `trade-panel.tsx` listing label in error |

**BFF tx routes** (`app/api/trade/tx/`):

| Route | Origin gate | Write gate | Wallet challenge | SDK writePath | Notes |
|-------|-------------|------------|------------------|---------------|-------|
| `buy` | yes | yes | yes (server) | SDK fill | Green |
| `list` | yes | yes | yes (server) | SDK list | Green |
| `delist` | yes | yes | yes (server) | SDK delist | Green — security inv1 landed |
| `bid` | yes | yes | yes (server) | REST + `makerBroker` | Green |
| `cancel-bid` | yes | yes | yes (server) | REST | Green |
| `challenge` | n/a | n/a | issues challenges | n/a | Green |

**M3 focus — staging fill / broker / fees:**

- `buildFillTransaction` + `takerBroker` via `getSvfBrokerPubkey()` in `tensor-tcm-sdk.ts` — **wired**
- `attachBrokerFeeAccounts` in `tensor-fees.ts` — **stub** (TC-075); blocked on broker PDA + fees IDL codegen
- Broker PDA ops checklist — all `[ ]` — **operator-blocked**, no spawn
- Staging checklist pre-flight + verification — all `[ ]` — **operator-blocked**
- `programs/idl-lock.json` — commit pinned (P2-INT-OC05 partial; submodule init still ops)

**Client write gates:** `isTradeWriteEnabledClient()` on all trade modals/hooks — green.

**New gap — client wallet challenge:** Server routes enforce `TRADE_TX_REQUIRE_WALLET_CHALLENGE` when flag set, but **no client hook** calls `/api/trade/tx/challenge` or appends `challengeId`/`walletSignature` to tx fetches (`use-tensor-buy`, `use-tensor-list`, `use-tensor-delist`, `use-tensor-bid`, `use-tensor-cancel-bid`). Staging hardening with flag on will fail from UI until wired.

**Backlog (P2 INT on-chain):**

| ID | Status | Notes |
|----|--------|-------|
| P2-INT-OC05 | PARTIAL | `idl-lock.json` pinned; submodule clone ops pending |
| P2-INT-OC06 | TODO | Broker PDA devnet — operator |
| P2-INT-OC07 | PARTIAL | Fill tx wired; fees accounts stub (TC-075) |
| P2-INT-OC08 | TODO | Whitelist CC + Phygitals mints on TL1ST |
| P2-INT-OC09 | TODO | `slabvault-broker` devnet deploy |
| P2-INT-T09 | TODO | BFF Tensor cache — **backend lane** |
| P2-INT-T12 | TODO | Staging `RWA_TRADE_ENABLED` — trade shell exists; vault list tx unverified |
| P2-INT-T16 | TODO | Buy/accept bid tx verify hook — `marketplace-payment-verify.ts` patterns not wired to `/trade` |

**Tx tests (this invocation):**

- `npx tsx --test tests/tensor-tcm.test.ts tests/api-trade-tx.test.ts tests/api-trade-tx-guardrails.test.ts` — **71 pass / 0 fail** (~61s)

**Prod contract:** `.env.example` keeps `TENSOR_TRADE_WRITE_ENABLED=false`.

---

### Gaps

| ID | Gap | Severity | Owner | Action |
|----|-----|----------|-------|--------|
| G6 | Delist route wallet challenge | P1 | security | **fixed** (server; security inv1) |
| G7 | Broker PDA registration on Tensor Fees | P0 | ops | deferred (operator) |
| G8 | Staging checklist pre-flight + first fill | P0 | ops/staging | deferred (operator) |
| G9 | `attachBrokerFeeAccounts` stub (TC-075) | P1 | eng | deferred until broker PDA + fees IDL |
| G10 | CSRF N/A on GET tx routes | P2 | security | **done** (documented) |
| G11 | Client hooks missing wallet challenge flow when `TRADE_TX_REQUIRE_WALLET_CHALLENGE=true` | P1 | `components/trade/tensor/*`, `use-tensor-tx.ts` | deferred — cap reached |
| G12 | P2-INT-T16 tx verify after buy/bid send | P1 | `lib/marketplace-payment-verify.ts`, trade hooks | deferred — cap reached |
| G13 | P2-INT-OC08 whitelist mints on TL1ST | P2 | `lib/onchain/` | deferred — cap reached |

---

### Spawned workers

**cap reached (concurrency cap)** — global in-flight 24/24; **0 spawns** this iteration.

Prior on-chain workers (oc-bg-1..oc-bg-5) merged; journal marked complete above.

---

### Inline fixes

none — env-gate copy and fees preview docs landed in invocation 2

---

### Iteration footer

- **Spawned count:** 0 (cap reached)
- **Remaining gaps:** G7–G9 (operator/TC-075), G11–G13 (eng, queued for next tick when cap clears)
- **Recommend next lane:** **operator** broker PDA + staging env → **onchain** (G11 client challenge wiring, G12 verify hook) when global cap < 24 → **QA** full verify pass
- **Stop reason:** 1 iteration complete; global cap 24/24; broker PDA operator-blocker; tx tests green ×3 consecutive (inv 1–3 focused suite)

---

## End of invocation 3

**Total iterations (today):** 3  
**Total spawned (invocation 3):** 0  
**Global cap at stop:** 24/24 in-flight  
**Green criteria met:** partial — tx wiring + focused tests green ×3; **staging checklist incomplete**; broker PDA ops pending; client challenge wiring open (G11)  
**Operator action needed:** Broker PDA registration, staging write flags, first mainnet fill dry-run per [trade-staging-checklist.md](../../trade-staging-checklist.md)

## Invocation 4 — 2026-05-22T22:15:00Z

**Iteration:** 1 of 1  
**Elapsed:** ~10m  
**Global in-flight fix workers (before spawn):** 24 — segment bg-1..9 (9), ui ui-bg-6..10 (5), backend bg-6..10 (5), security sec-bg-1..5 (5); onchain oc-bg-1..5 **complete** — **cap reached (24/24)**

---

### Audit summary

**BFF tx routes** (`app/api/trade/tx/`) — unchanged green from invocation 3:

| Route | Origin gate | Write gate | Wallet challenge (server) | SDK writePath | Notes |
|-------|-------------|------------|---------------------------|---------------|-------|
| `buy` | yes | yes | yes | SDK fill + `takerBroker` | Green |
| `list` | yes | yes | yes | SDK list | Green |
| `delist` | yes | yes | yes | SDK delist | Green |
| `bid` | yes | yes | yes | REST + `makerBroker` default | Green |
| `cancel-bid` | yes | yes | yes | REST | Green |
| `challenge` | n/a | n/a | issues challenges | n/a | Green |

**M3 focus — staging fill / broker PDA / fees on fill:**

- `buildFillTransaction` → `takerBroker` via `getSvfBrokerPubkey()` in `tensor-tcm-sdk.ts` — **wired**
- `attachBrokerFeeAccounts` in `tensor-fees.ts` — **stub** (TC-075); blocked on broker PDA registration + fees IDL codegen
- Broker PDA ops checklist — all `[ ]` — **operator-blocked**, no spawn
- Staging checklist pre-flight + verification — all `[ ]` — **operator-blocked**
- `programs/idl-lock.json` — commit pinned (P2-INT-OC05 partial; submodule clone ops pending)

**Client gap (G11):** `use-tensor-tx.ts` `fetchTensorTxRoute` does not call `/api/trade/tx/challenge` or append `challengeId`/`walletSignature` when `TRADE_TX_REQUIRE_WALLET_CHALLENGE=true`. Staging hardening with flag on will fail from UI until wired.

**Whitelist (P2-INT-OC08):** `SLABVAULT_TRADE_COLLECTIONS` CC + Phygitals `collectionMint: null`, `whitelistOnTensor: false` — DAS-confirmed mints + TL1ST whitelist still ops/eng.

**Backlog (P2 INT on-chain):**

| ID | Status | Notes |
|----|--------|-------|
| P2-INT-OC05 | PARTIAL | `idl-lock.json` pinned; submodule clone ops pending |
| P2-INT-OC06 | TODO | Broker PDA devnet — operator |
| P2-INT-OC07 | PARTIAL | Fill tx wired; fees accounts stub (TC-075) |
| P2-INT-OC08 | TODO | Whitelist CC + Phygitals mints on TL1ST |
| P2-INT-OC09 | TODO | `slabvault-broker` devnet deploy |
| P2-INT-T09 | TODO | BFF Tensor cache — **backend lane** (deferred) |
| P2-INT-T12 | TODO | Staging `RWA_TRADE_ENABLED` — trade shell live; vault list tx unverified |
| P2-INT-T16 | TODO | Buy/accept bid tx verify — `marketplace-payment-verify.ts` not wired to trade hooks |

**Tx tests (this invocation):**

- `npx tsx --test tests/tensor-tcm.test.ts tests/api-trade-tx.test.ts tests/api-trade-tx-guardrails.test.ts` — **71 pass / 0 fail** (~52s)

**Prod contract:** `.env.example` keeps `TENSOR_TRADE_WRITE_ENABLED=false`.

---

### Gaps

| ID | Gap | Severity | Owner | Action |
|----|-----|----------|-------|--------|
| G7 | Broker PDA registration on Tensor Fees | P0 | ops | deferred (operator) |
| G8 | Staging checklist pre-flight + first fill | P0 | ops/staging | deferred (operator) |
| G9 | `attachBrokerFeeAccounts` stub (TC-075) | P1 | eng | deferred until broker PDA + fees IDL |
| G11 | Client hooks missing wallet challenge flow | P1 | `use-tensor-tx.ts`, tensor hooks | deferred — cap reached |
| G12 | P2-INT-T16 tx verify after buy/bid send | P1 | `marketplace-payment-verify.ts`, trade hooks | deferred — cap reached |
| G13 | P2-INT-OC08 whitelist mints on TL1ST | P2 | `lib/onchain/collections.ts` | deferred — cap reached |
| G14 | P2-INT-T12 vault list tx staging verify | P2 | `use-tensor-list.ts`, staging | deferred — cap reached |
| G15 | P2-INT-OC09 slabvault-broker devnet deploy | P2 | ops/Anchor CI | deferred (operator) |

---

### Spawned workers

**cap reached (concurrency cap)** — global in-flight 24/24; **0 spawns** this iteration.

Queued for next tick when cap clears: G11 (client challenge), G12 (tx verify hook), G13 (whitelist mints), G14 (list tx verify).

---

### Inline fixes

none — env-gate copy and fees preview docs landed in invocations 2–3

---

### Iteration footer

- **Spawned count:** 0 (cap reached)
- **Remaining gaps:** G7–G9 (operator/TC-075), G11–G15 (eng, queued)
- **Recommend next lane:** **operator** broker PDA + staging env → **onchain** (G11–G14) when global cap < 24 → **QA** verify pass
- **Stop reason:** 1 iteration complete; global cap 24/24; broker PDA operator-blocker; tx tests green ×4 consecutive (inv 1–4 focused suite)

---

## End of invocation 4

**Total iterations (today):** 4  
**Total spawned (invocation 4):** 0  
**Global cap at stop:** 24/24 in-flight  
**Green criteria met:** partial — tx wiring + focused tests green ×4; **staging checklist incomplete**; broker PDA ops pending; client challenge wiring open (G11)  
**Operator action needed:** Broker PDA registration, staging write flags, first staging fill dry-run per [trade-staging-checklist.md](../../trade-staging-checklist.md)

---

## Status: active
