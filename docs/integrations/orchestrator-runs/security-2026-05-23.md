# Security orchestrator — 2026-05-23

## Invocation 1 — 2026-05-23T00:00:00Z

**Iteration:** 1 of 1  
**Elapsed:** ~2m  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path

### Audit summary

- `npm run test:guardrails`: **85/85 pass**, 0 fail (~50s)
- `npm run ops:env`: **3 ERROR** (placeholder `NEXTAUTH_SECRET`, `ADMIN_PASSWORD`, `CRON_SECRET` — local dev expected); **1 WARN** (`SYNC_API_TOKEN` unset → sync falls back to `ADMIN_PASSWORD`)
- `npm run ops:env:production`: **3 ERROR** (`NEXT_PUBLIC_SITE_URL`, `CRON_SECRET` missing/placeholder); **3 WARN** (`RESERVE_REQUIRE_WALLET_CHALLENGE`, `TRADE_TX_REQUIRE_WALLET_CHALLENGE`, Upstash KV); P0-SEC-09 WARN removed inline (unset defaults on in production runtime)
- CSRF on `/api/trade/tx/*`: **N/A (GET-only)** — policy is origin gate (`requireTrustedTensorTxOrigin`) + optional wallet challenge + proxy rate limits; not `requireCsrfProtection` (POST-only). All 5 tx builders + challenge route wired for origin gate after inline fix.
- Wallet challenge: **complete on all 5 tx routes** (buy, list, bid, delist, cancel-bid); challenge issuer at `/api/trade/tx/challenge`
- Write gates: `TENSOR_TRADE_WRITE_ENABLED=false` returns 503 before wallet challenge — guardrail tests pass (M3 honest buy gates preserved)
- `SECURITY_CHECKLIST.md`: GRAILS section green; admin RBAC partial (slabs GET gap found this iteration)
- Global in-flight fix workers (today, before spawn): **0** — meta tick 1 spawned 6 lane orchestrators; security headroom **5/24**

### Backlog focus (P0-SEC-08–15, P1-WAL-05–06, P2-SEC-01–03)

| ID | Status | Notes |
|----|--------|-------|
| P0-SEC-08 | OPEN | Upstash/KV not wired locally; `checkRateLimitUpstash` in `lib/security.ts` ready — operator must set `KV_REST_API_URL`+`TOKEN` in prod |
| P0-SEC-09 | PARTIAL | `TRANSACTION_ACCESS_REQUIRE_WALLET_SIGNATURE` defaults on in production runtime; ops:env contract reconciled inline |
| P0-SEC-10 | OPEN | `RESERVE_REQUIRE_WALLET_CHALLENGE` env-only; production WARN |
| P0-SEC-11 | OPEN | Shared `ADMIN_PASSWORD` model — deferred (operator) |
| P0-SEC-12 | PARTIAL | All admin handlers now call `requireAdminRole`; slabs GET was last gap — fixed inline; bearer JWT fallback transitional |
| P0-SEC-13 | DONE | Checklist reconciled |
| P0-SEC-14 | DONE | `npm run ops:rotation:check` + runbook §6 |
| P0-SEC-15 | DONE | `docs/runbooks/custody-fulfillment-incidents.md` |
| P1-WAL-05 | OPEN | `TRADE_TX_REQUIRE_WALLET_CHALLENGE` not enabled — staging WARN |
| P1-WAL-06 | PARTIAL | Purchases list signed access implemented; enable in prod |
| P2-SEC-01 | DONE | Partner webhook auth + guardrails |
| P2-SEC-02 | DONE | `SCRAPER_MAX_RESPONSE_BYTES` |
| P2-SEC-03 | OPEN | Robots/TOS per source — Phase 4 deferred |

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G1 | Admin slabs GET missing `requireAdminRole` | P0 | `app/api/admin/slabs/route.ts` | **inline fixed** |
| G2 | Challenge route missing origin gate | P1 | `app/api/trade/tx/challenge/route.ts` | **inline fixed** |
| G3 | ops:env misleading P0-SEC-09 WARN when flag unset in production | P1 | `scripts/check-env-contract.ts` | **inline fixed** + spawn test |
| G4 | No guardrail test for admin slabs GET RBAC | P1 | `tests/api-admin-guardrails.test.ts` | spawn |
| G5 | No guardrail test for challenge origin gate | P1 | `tests/api-trade-tx-guardrails.test.ts` | spawn |
| G6 | No unit test for Upstash rate limit path | P1 | `lib/security.ts`, tests | spawn (P0-SEC-08) |
| G7 | Upstash durable rate limits not in production | P0 | operator env | deferred (P0-SEC-08) |
| G8 | Per-user admin accounts | P0 | admin auth, NextAuth | deferred (P0-SEC-11) |
| G9 | SECURITY_CHECKLIST admin RBAC status stale | P2 | `SECURITY_CHECKLIST.md` | spawn |

### Spawned workers

| Worker | Gap ID | Task title | Agent ID | Status |
|--------|--------|------------|----------|--------|
| sec-bg-1 | G4 | Security — admin slabs GET RBAC guardrail test | `a3ad7959-debd-41a7-aca0-90f4f1b4317e` | spawned (not awaited) |
| sec-bg-2 | G5 | Security — challenge route origin gate guardrail test | `912c86c4-f450-4f75-bb70-85f14846dc63` | spawned (not awaited) |
| sec-bg-3 | G6 | Security — Upstash checkRateLimit unit test | `dc0efdd9-82e8-448b-b1db-978ec0728e5e` | spawned (not awaited) |
| sec-bg-4 | G3 | Security — ops:env P0-SEC-09 contract test | `94b446d0-4b6e-403c-a2e6-4e31b7df014b` | spawned (not awaited) |
| sec-bg-5 | G9 | Security — SECURITY_CHECKLIST P0-SEC-12 reconcile | `429d52cc-e6bb-45e0-a96a-08ec7531a485` | spawned (not awaited) |

**Spawned count:** 5 (max 5 this iteration)

### Inline fixes

- `app/api/admin/slabs/route.ts` — `requireAdminRole(request, ["admin"])` on GET (P0-SEC-12)
- `app/api/trade/tx/challenge/route.ts` — `requireTrustedTensorTxOrigin` before challenge issue (parity with tx builders)
- `scripts/check-env-contract.ts` — P0-SEC-09: no production WARN when flag unset (runtime default on); ERROR when explicitly false in production mode

### Iteration footer

- **Remaining gaps:** 4 deferred operator items (G7–G8, P0-SEC-10/11 prod env), 5 worker gaps (G4–G6, G3 test, G9 docs)
- **Global in-flight after spawn:** **5/24** (security sec-bg-1..5)
- **Recommend next lane:** **security** (meta re-invoke after workers land) for P0-SEC-11 per-user admin spike, or **onchain** for M3 staging write rehearsal + client wallet challenge flow
- **Stop reason:** 1 iteration complete; guardrails green (0 fail ×1 — need ×2 for lane green stop)

---

## End of invocation

**Total iterations:** 1  
**Total spawned:** 5 (background, not awaited)  
**Green criteria met:** partial — `npm run test:guardrails` 0 fail (1 consecutive; need 2)  
**Operator action needed:** Replace placeholder secrets in `.env.local`; set `SYNC_API_TOKEN`; enable `RESERVE_REQUIRE_WALLET_CHALLENGE`, `TRADE_TX_REQUIRE_WALLET_CHALLENGE`, and Upstash KV in staging/production

---

## Invocation 2 — 2026-05-23T10:40:00Z

**Meta tick:** 2  
**Iteration:** 1 of 1  
**Elapsed:** ~45m  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path

### Audit summary

- `npm run test:guardrails`: **95/95 pass**, 0 fail (~354s) — up from 85/85 inv1 (sec-bg workers landed: admin slabs GET RBAC, challenge origin gate, Upstash rate limit, P0-SEC-09 contract, checklist reconcile)
- `npm run ops:env`: **3 ERROR** (placeholder `NEXTAUTH_SECRET`, `ADMIN_PASSWORD`, `CRON_SECRET` — local dev expected); **2 WARN** (`SYNC_API_TOKEN` unset; P0-SEC-09 dev unset note)
- `npm run ops:env:production`: **3 ERROR** (`NEXT_PUBLIC_SITE_URL`, `CRON_SECRET` missing/placeholder); **4 WARN** (`RESERVE_REQUIRE_WALLET_CHALLENGE`, `TRADE_TX_REQUIRE_WALLET_CHALLENGE`, Upstash KV, `SYNC_API_TOKEN` fallback)
- CSRF on `/api/trade/tx/*`: **N/A (GET-only)** — origin gate on all 6 routes (5 builders + challenge); wallet challenge on 5 builders; write gate 503 when disabled
- Partner ingest BFF: `/api/trade/partners/[platform]/listings` + `/stats` — public read OK; **`?live=1` unauthenticated scrape trigger** identified as abuse vector (cross-ref backend north-star ingest)
- `SECURITY_CHECKLIST.md`: GRAILS + admin RBAC green; partner live-scrape auth item added
- Global in-flight fix workers (2026-05-23, before spawn): **25** — segment 5, ui 5, backend 5, onchain 5, security 5 — **`cap reached (concurrency cap)`**

### Backlog focus (P0-SEC-08–15, P1-WAL-05–06, P2-SEC-01–03)

| ID | Status | Notes |
|----|--------|-------|
| P0-SEC-08 | OPEN | Upstash/KV not wired; unit tests now green (ok 88–91); operator must set prod env |
| P0-SEC-09 | PARTIAL | Contract tests green; production runtime defaults on when unset |
| P0-SEC-10 | OPEN | `RESERVE_REQUIRE_WALLET_CHALLENGE` production WARN |
| P0-SEC-11 | OPEN | Shared `ADMIN_PASSWORD` model — deferred (operator) |
| P0-SEC-12 | DONE | All admin handlers + guardrail tests green |
| P0-SEC-13 | DONE | Checklist reconciled |
| P0-SEC-14 | DONE | Rotation check + runbook |
| P0-SEC-15 | DONE | Custody incident runbook |
| P1-WAL-05 | OPEN | `TRADE_TX_REQUIRE_WALLET_CHALLENGE` not enabled — staging WARN |
| P1-WAL-06 | PARTIAL | Purchases signed access implemented; enable in prod |
| P2-SEC-01 | DONE | Partner webhook auth + guardrails |
| P2-SEC-02 | DONE | `SCRAPER_MAX_RESPONSE_BYTES` |
| P2-SEC-03 | OPEN | Robots/TOS per source — Phase 4 deferred |

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G10 | Partner BFF `?live=1` allowed unauthenticated live scrape in production | P1 | `app/api/trade/partners/[platform]/*/route.ts` | **inline fixed** — `requireSyncAuth` when `live=1` + `NODE_ENV=production` |
| G11 | No guardrail test for partner `?live=1` auth gate | P1 | `tests/api-trade-partner-listings.test.ts` | deferred — cap reached; spawn next tick |
| G12 | Upstash durable rate limits not in production | P0 | operator env | deferred (P0-SEC-08) |
| G13 | Per-user admin accounts | P0 | admin auth, NextAuth | deferred (P0-SEC-11) |
| G14 | Wallet challenge flags not enabled in staging/prod | P1 | env contract | deferred (P0-SEC-10 / P1-WAL-05) |

### Spawned workers

| Worker | Gap ID | Task title | Agent ID | Status |
|--------|--------|------------|----------|--------|
| — | — | — | — | **`cap reached (concurrency cap)`** — 0 spawns (25/24 in-flight) |

**Spawned count:** 0 (max 5 this iteration; global cap full)

### Inline fixes

- `app/api/trade/partners/[platform]/listings/route.ts` — `requireSyncAuth` gate on `?live=1` in production (north-star ingest abuse prevention)
- `app/api/trade/partners/[platform]/stats/route.ts` — same gate
- `SECURITY_CHECKLIST.md` — partner BFF live-scrape auth documented

### Iteration footer

- **Remaining gaps:** G11 guardrail test (next tick after cap drains), G12–G14 operator/env items
- **Global in-flight after spawn:** **25/24** (unchanged)
- **Recommend next lane:** **security** after cap drains (G11 partner live-scrape guardrail test) or **backend** for ingest aggregation; operator: enable Upstash + wallet challenge flags in staging
- **Stop reason:** 1 iteration complete; guardrails green 0 fail ×2 consecutive (85/85 inv1 → 95/95 inv2)

---

## End of invocation 2

**Total iterations:** 1  
**Total spawned:** 0 (`cap reached`)  
**Green criteria met:** yes — `npm run test:guardrails` 0 fail for **2 consecutive** security invocations  
**Operator action needed:** drain 25 in-flight workers; replace placeholder secrets; set `SYNC_API_TOKEN`; enable `RESERVE_REQUIRE_WALLET_CHALLENGE`, `TRADE_TX_REQUIRE_WALLET_CHALLENGE`, Upstash KV in staging/production

---

## Invocation 3 — 2026-05-23T11:38:00Z

**Meta tick:** 3  
**Iteration:** 1 of 1  
**Elapsed:** ~12m  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path

### Audit summary

- `npm run test:guardrails`: **95/95 pass**, 0 fail (~207s)
- `npm run ops:env`: **3 ERROR** (placeholder `NEXTAUTH_SECRET`, `ADMIN_PASSWORD`, `CRON_SECRET` — local dev expected); **2 WARN** (`SYNC_API_TOKEN` unset; P0-SEC-09 dev unset note)
- `npm run ops:env:production`: **3 ERROR** (`NEXT_PUBLIC_SITE_URL`, `CRON_SECRET` missing/placeholder); **4 WARN** (`RESERVE_REQUIRE_WALLET_CHALLENGE`, `TRADE_TX_REQUIRE_WALLET_CHALLENGE`, Upstash KV, `SYNC_API_TOKEN` fallback)
- CSRF on `/api/trade/tx/*`: **N/A (GET-only)** — origin gate on all 6 routes (5 builders + challenge); wallet challenge on 5 builders; write gate 503 when disabled
- Partner BFF `?live=1`: auth gate present on listings + stats (inv2 inline); **G11 guardrail tests added inline** this tick (cap blocked spawn)
- `SECURITY_CHECKLIST.md`: GRAILS + admin RBAC + partner live-scrape auth green
- Global in-flight fix workers (2026-05-23, before spawn): **25** — segment seg-bg-20..24 (5), ui ui-bg-1..5 (5), backend be-bg-1..5 (5), onchain oc-bg-1..5 (5), security sec-bg-1..5 (5) — **`cap reached (concurrency cap)`** (workers appear landed in tree; journals not reconciled)

### Backlog focus (P0-SEC-08–15, P1-WAL-05–06, P2-SEC-01–03)

| ID | Status | Notes |
|----|--------|-------|
| P0-SEC-08 | OPEN | Upstash/KV not wired in prod; unit tests green |
| P0-SEC-09 | PARTIAL | Contract tests green; runtime defaults on when unset |
| P0-SEC-10 | OPEN | `RESERVE_REQUIRE_WALLET_CHALLENGE` production WARN |
| P0-SEC-11 | OPEN | Shared `ADMIN_PASSWORD` model — deferred (operator) |
| P0-SEC-12 | DONE | All admin handlers + guardrail tests green |
| P0-SEC-13 | DONE | Checklist reconciled |
| P0-SEC-14 | DONE | Rotation check + runbook |
| P0-SEC-15 | DONE | Custody incident runbook |
| P1-WAL-05 | OPEN | `TRADE_TX_REQUIRE_WALLET_CHALLENGE` not enabled — staging WARN |
| P1-WAL-06 | PARTIAL | Purchases signed access implemented; enable in prod |
| P2-SEC-01 | DONE | Partner webhook auth + guardrails |
| P2-SEC-02 | DONE | `SCRAPER_MAX_RESPONSE_BYTES` |
| P2-SEC-03 | OPEN | Robots/TOS per source — Phase 4 deferred |

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G11 | Partner BFF `?live=1` auth guardrail test | P1 | `tests/api-trade-partner-listings.test.ts` | **inline fixed** — 401 tests for listings + stats in production |
| G12 | Upstash durable rate limits not in production | P0 | operator env | deferred (P0-SEC-08) |
| G13 | Per-user admin accounts | P0 | admin auth, NextAuth | deferred (P0-SEC-11) |
| G14 | Wallet challenge flags not enabled in staging/prod | P1 | env contract | deferred (P0-SEC-10 / P1-WAL-05) |
| G15 | Robots/TOS per ingest source | P2 | scraper policy | deferred (P2-SEC-03) |

### Spawned workers

| Worker | Gap ID | Task title | Agent ID | Status |
|--------|--------|------------|----------|--------|
| — | — | — | — | **`cap reached (concurrency cap)`** — 0 spawns (25/24 in-flight) |

**Spawned count:** 0 (max 5 this iteration; global cap full)

### Inline fixes

- `tests/api-trade-partner-listings.test.ts` — guardrail tests: partner listings/stats GET reject `?live=1` without sync bearer in production (G11 closed); **8/8 pass** on focused run

### Iteration footer

- **Remaining gaps:** G12–G15 operator/env/deferred items; global worker journal drain before next spawns
- **Global in-flight after spawn:** **25/24** (unchanged)
- **Recommend next lane:** **security** after cap drains (P0-SEC-11 per-user admin spike) or **qa** (confirmatory full suite); operator: enable Upstash + wallet challenge flags in staging
- **Stop reason:** 1 iteration complete; guardrails green 0 fail ×3 consecutive security invocations

---

## End of invocation 3

**Total iterations:** 1  
**Total spawned:** 0 (`cap reached`)  
**Green criteria met:** yes — `npm run test:guardrails` 0 fail for **3 consecutive** security invocations  
**Operator action needed:** reconcile in-flight worker journals; replace placeholder secrets; set `SYNC_API_TOKEN`; enable `RESERVE_REQUIRE_WALLET_CHALLENGE`, `TRADE_TX_REQUIRE_WALLET_CHALLENGE`, Upstash KV in staging/production

---

## Invocation 4 — 2026-05-23T12:35:00Z

**Meta tick:** 4  
**Iteration:** 1 of 1  
**Elapsed:** ~15m  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path

### Audit summary

- `npm run test:guardrails`: **86/87 pass**, **1 fail** (~168s) — `tests/api-admin-guardrails.test.ts` module load flake: `data/site.json: Unexpected end of JSON input` (static import in `lib/data-sync.ts` races concurrent sync writes); isolated re-run **13/13 pass**
- `npm run ops:env`: **3 ERROR** (placeholder `NEXTAUTH_SECRET`, `ADMIN_PASSWORD`, `CRON_SECRET` — local dev expected); **2 WARN** (`SYNC_API_TOKEN` unset; P0-SEC-09 dev unset note)
- `npm run ops:env:production`: **3 ERROR** (`NEXT_PUBLIC_SITE_URL`, `CRON_SECRET` missing/placeholder); **4 WARN** (`RESERVE_REQUIRE_WALLET_CHALLENGE`, `TRADE_TX_REQUIRE_WALLET_CHALLENGE`, Upstash KV, `SYNC_API_TOKEN` fallback)
- CSRF on `/api/trade/tx/*`: **N/A (GET-only)** — origin gate on all 6 routes (5 builders + challenge); wallet challenge on 5 builders; write gate 503 when disabled
- Partner BFF `?live=1`: auth gate present on listings + stats (inv2 inline); guardrail tests added inv3 — **spawn sec-bg-10** to verify/harden coverage
- `SECURITY_CHECKLIST.md`: GRAILS + admin RBAC + partner live-scrape auth green
- Global in-flight fix workers (2026-05-23, before spawn): **15** — segment seg-bg-25..29 (5), backend be-bg-6..10 (5), onchain oc-bg-6..10 (5); tick 1–2 batches drained in code — **headroom 9/24**

### Backlog focus (P0-SEC-08–15, P1-WAL-05–06, P2-SEC-01–03)

| ID | Status | Notes |
|----|--------|-------|
| P0-SEC-08 | OPEN | Upstash/KV not wired in prod; unit tests green |
| P0-SEC-09 | PARTIAL | Contract tests green; runtime defaults on when unset |
| P0-SEC-10 | OPEN | `RESERVE_REQUIRE_WALLET_CHALLENGE` production WARN — contract test gap |
| P0-SEC-11 | OPEN | Shared `ADMIN_PASSWORD` model — deferred (operator) |
| P0-SEC-12 | DONE | All admin handlers + guardrail tests green |
| P0-SEC-13 | DONE | Checklist reconciled |
| P0-SEC-14 | DONE | Rotation check + runbook |
| P0-SEC-15 | DONE | Custody incident runbook |
| P1-WAL-05 | OPEN | `TRADE_TX_REQUIRE_WALLET_CHALLENGE` not enabled — staging WARN; contract test gap |
| P1-WAL-06 | PARTIAL | Purchases signed access implemented; enable in prod |
| P2-SEC-01 | DONE | Partner webhook auth + guardrails |
| P2-SEC-02 | DONE | `SCRAPER_MAX_RESPONSE_BYTES` |
| P2-SEC-03 | OPEN | Robots/TOS per source — Phase 4 deferred |

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G16 | `data-sync` static `site.json` import races concurrent sync writes → guardrail flake | P1 | `lib/data-sync.ts`, tests | spawn sec-bg-6 |
| G17 | Missing ops:env contract test for P0-SEC-10 `RESERVE_REQUIRE_WALLET_CHALLENGE` | P1 | `tests/check-env-contract.test.ts` | spawn sec-bg-7 |
| G18 | Missing ops:env contract test for P1-WAL-05 `TRADE_TX_REQUIRE_WALLET_CHALLENGE` | P1 | `tests/check-env-contract.test.ts` | spawn sec-bg-8 |
| G19 | Missing ops:env contract test for `SYNC_API_TOKEN` ≠ `ADMIN_PASSWORD` | P1 | `tests/check-env-contract.test.ts` | spawn sec-bg-9 |
| G20 | Partner `?live=1` guardrail hardening / missing bearer cases | P1 | `tests/api-trade-partner-listings.test.ts` | spawn sec-bg-10 |
| G12 | Upstash durable rate limits not in production | P0 | operator env | deferred (P0-SEC-08) |
| G13 | Per-user admin accounts | P0 | admin auth, NextAuth | deferred (P0-SEC-11) |
| G14 | Wallet challenge flags not enabled in staging/prod | P1 | env contract | deferred (operator) |
| G15 | Robots/TOS per ingest source | P2 | scraper policy | deferred (P2-SEC-03) |

### Spawned workers

| Worker | Gap ID | Task title | Agent ID | Status |
|--------|--------|------------|----------|--------|
| sec-bg-6 | G16 | Security — data-sync site.json import race fix | `0853f388-a903-454b-8bf6-6211db3ae9f6` | spawned (not awaited) |
| sec-bg-7 | G17 | Security — ops:env P0-SEC-10 contract test | `537ca8c7-172c-4dff-878d-c749bfe76513` | spawned (not awaited) |
| sec-bg-8 | G18 | Security — ops:env P1-WAL-05 contract test | `4525a359-a5ef-44e3-ae1a-390d3efe1dd5` | spawned (not awaited) |
| sec-bg-9 | G19 | Security — ops:env SYNC_API_TOKEN contract test | `2d8ad012-9c53-49e1-8ff0-c8ae3e9f0359` | spawned (not awaited) |
| sec-bg-10 | G20 | Security — partner live-scrape guardrail hardening | `cfd8655b-0b5f-4302-b555-98ad42a12266` | spawned (not awaited) |

**Spawned count:** 5 (max 5 this iteration)

### Inline fixes

- none this tick — G16 deferred to sec-bg-6 (non-trivial lazy-load refactor)

### Iteration footer

- **Remaining gaps:** G12–G15 operator/deferred; 5 worker gaps in flight (G16–G20)
- **Global in-flight after spawn:** **20/24** (tick 3 lanes 15 + security sec-bg-6..10)
- **Recommend next lane:** **security** after workers land (re-run guardrails for 0-fail streak) or **qa** confirmatory; operator: enable Upstash + wallet challenge flags in staging
- **Stop reason:** 1 iteration complete; guardrails **1 fail** (flake — breaks 3× green streak until sec-bg-6 lands)

---

## End of invocation 4

**Total iterations:** 1  
**Total spawned:** 5 (background, not awaited)  
**Green criteria met:** no — `npm run test:guardrails` 86/87 (1 flake; need 0 fail ×2 for lane green stop)  
**Operator action needed:** replace placeholder secrets; set distinct `SYNC_API_TOKEN`; enable `RESERVE_REQUIRE_WALLET_CHALLENGE`, `TRADE_TX_REQUIRE_WALLET_CHALLENGE`, Upstash KV in staging/production
