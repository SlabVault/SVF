# Backend orchestrator — 2026-05-23

## Invocation 1 — 2026-05-23T00:00:00Z

**Iteration:** 1 of 1  
**Elapsed:** ~15m  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path  
**Priority alignment:** (1) DB+batch sync, (2) API-first CC ingest, (3) aggregation hardening, (4) deep links on every partner row

### Audit summary

- **Global in-flight fix workers (2026-05-23, before spawn):** 10 — segment seg-bg-20..24 (5), security sec-bg-1..5 (5). **Cap headroom: 14/24**
- **`DATABASE_URL`:** UNSET in orchestrator shell — JSON seed fallback path exercised; live `ExternalListing` upsert requires operator env + `npm run db:preflight` (P0-DAT-04 / P0-OPS-09)
- **Partner ingest read path:** `lib/partner-listings.ts` — DB → JSON → partner API (`fetchPartnerApiRows`) → CC scrape fallback (`shouldUseLiveScrapeFallback`) → Helius DAS → optional Tensor enrichment
- **Batch sync:** `fetchCollectorCryptIngestListings()` API-first then scrape (`lib/collector-crypt-live-listings.ts`); `syncExternalListingsToJson` upserts DB + writes JSON; returns `ccIngestSource`, `dbConfigured`, `dbUpsertCount`
- **Adapters:** `lib/partner-ingest-adapter.ts` — CC + Phygitals live; Beezie/Courtyard preview stubs (M6)
- **`ExternalListing` model:** matches usage in `lib/external-listings.ts` / `prisma/schema.prisma`
- **Cron:** `vercel.json` POST `/api/sync` every 30m → `external-listings:discover` via `lib/data-sync.ts` (P2-INT-D10 DONE)
- **Landing / desk:** `/trade` + `/trade/c/collector-crypt` + `/trade/c/phygitals` load partner ingest without Tensor key; `/trade/all` via `loadAllTradeListings()` + BFF `GET /api/trade/all/listings`
- **Deep links:** `resolvePartnerDeepLink` + `mapPartnerListingToTradeListing` upgrade generic CC marketplace URLs to cert search; CC unit tests present; Phygitals + BFF row assertions **gap**
- **Aggregation:** `dedupePartnerTradeListings`, `alternateVenueAsks`, venue tags in `lib/trade/all-listings.ts` — tests green per 2026-05-22 inv 4; BFF missing per-row deep link assertions
- **Phygitals:** `PHYGITALS_LIVE_INGEST_AVAILABLE=false` (seed-only, honest stub)
- **CC API:** `COLLECTOR_CRYPT_MARKETPLACE_API_AVAILABLE=false`; API candidates tried on read + batch paths; scrape fallback when empty
- **Tensor:** optional enrichment only — core paths must not require `TENSOR_API_KEY`
- **Tests (this invocation):**
  - `tests/partner-listings.test.ts` — **28/28 pass** (~84s)
  - Full backend bundle not re-run (host slow); prior 2026-05-22 inv 4 reported trade-all-listings + external-listings-sync green

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G1 | Prod `ExternalListing` migration verify (P0-OPS-09 / P0-DAT-04) | P0 | operator / `npm run db:preflight` | deferred — operator |
| G2 | Partner BFF does not assert `vaultedUrl` on every CC/Phygitals row | P0 | `tests/api-trade-partner-listings.test.ts` | spawn be-bg-1 |
| G3 | `/api/trade/all/listings` missing per-row deep link + partner tests | P1 | `tests/api-trade-all-listings.test.ts` | spawn be-bg-2 |
| G4 | `resolvePartnerDeepLink` Phygitals unit coverage missing | P1 | `tests/partner-listings.test.ts` | spawn be-bg-3 |
| G5 | `fetchCollectorCryptIngestListings` API-first contract test missing | P1 | `lib/collector-crypt-live-listings.ts`, tests | spawn be-bg-4 |
| G6 | `ccIngestSource` not persisted in site sync metadata for ops | P1 | `lib/data-sync.ts`, `types/content.ts`, admin-ops | spawn be-bg-5 |
| G7 | Sync diagnostic detail omitted CC ingest source | P2 | `lib/data-sync.ts` | **inline fixed** |
| G8 | `sync:discover` runbook missing preflight pointer | P2 | `scripts/sync-external-listings.ts` | **inline fixed** |
| G9 | Phygitals live API ingest (P2-INT-PG03–06) | P2 | `lib/phygitals-listings.ts` | deferred — partner API unavailable |
| G10 | CC stable marketplace API (P2-INT-CC04–07) | P2 | `lib/collector-crypt-api-listings.ts` | deferred — partner stub |
| G11 | Magic Eden external listing ingest (P2-INT-D16) | P2 | aggregator Phase 4 | deferred |
| G12 | Grid/item deep-link CTA wiring | P1 | `components/trade/*` | deferred → **segment/UI lane** |

### Spawned workers

| Worker | Gap ID | Task title | Agent ID | Status |
|--------|--------|------------|----------|--------|
| be-bg-1 | G2 | Backend — partner BFF deep link assertions | `a50f3dc1-02cc-4a10-95b0-f51cbe96af61` | spawned (not awaited) |
| be-bg-2 | G3 | Backend — all-listings BFF deep link tests | `5ee57002-592c-49fa-b26c-03412dc34c13` | spawned (not awaited) |
| be-bg-3 | G4 | Backend — Phygitals resolvePartnerDeepLink tests | `1dd5a1d1-655e-44c6-a852-10080851e80a` | spawned (not awaited) |
| be-bg-4 | G5 | Backend — CC API-first ingest unit test | `3e0a21a3-5c83-4b11-83bf-aee5e265fe0a` | spawned (not awaited) |
| be-bg-5 | G6 | Backend — persist ccIngestSource sync metadata | `83b968e5-458f-486a-85d5-2418ced6d344` | spawned (not awaited) |

**Spawned count:** 5 (max 5 this iteration; global cap 24 — 10 in-flight + 5 = 15)  
**Inline fixes:**
- `lib/data-sync.ts` — sync diagnostic detail includes `ccIngestSource` (e.g. `CC 12 (api)`)
- `scripts/sync-external-listings.ts` — preflight runbook pointer (`npm run db:preflight`)

### Iteration footer

- **Remaining gaps:** 5 deferred (G1, G9–G12) + 5 in-flight workers (be-bg-1..5)
- **Global in-flight after spawn:** 15/24
- **Recommend next lane:** **qa** (verify pass after worker batch) or **segment** (grid deep-link CTA G12)
- **Stop reason:** LANE_ITERATIONS=1 complete; do not await workers

---

## End of invocation 1

**Total iterations:** 1  
**Total spawned this invocation:** 5 (be-bg-1..5, not awaited)  
**Green criteria met:** partial — ingest/sync code paths audited; deep-link + API-first test coverage queued to workers; prod DB preflight still operator-owned  
**Operator action needed:**
- Run `npm run db:preflight` on production (P0-DAT-04)
- Review be-bg-1..5 diffs when complete
- No commit unless operator approved

## Status: active

Pending worker output for BFF deep-link tests, Phygitals deep-link unit tests, CC API-first ingest test, and ccIngestSource ops metadata.

---

## Invocation 2 — 2026-05-23T11:15:00Z

**Meta tick:** 2  
**Iteration:** 1 of 1  
**Elapsed:** ~12m  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path  
**Priority alignment:** (1) DB+batch sync preflight, (2) API-first CC ingest, (3) `/trade/all` aggregation

### Audit summary

- **Global in-flight fix workers (2026-05-23, before spawn):** **25** — segment seg-bg-20..24 (5), onchain oc-bg-1..5 (5), backend be-bg-1..5 (5), ui ui-bg-1..5 (5), security sec-bg-1..5 (5). **Cap headroom: 0/24 → `cap reached (concurrency cap)`**
- **`DATABASE_URL`:** UNSET in orchestrator shell — JSON seed fallback only; prod upsert blocked until operator runs `npm run db:preflight` (P0-DAT-04 / P0-OPS-09)
- **Invocation 1 worker diffs (code review, not awaited):** be-bg-1..5 targets appear **landed in tree** — `assertEveryListingHasHttpsVaultedUrl` in partner + all-listings BFF tests; Phygitals `resolvePartnerDeepLink` unit tests; `fetchCollectorCryptIngestListings` API-first/scrape-fallback tests; `lastExternalListingsCcIngestSource` persisted in `lib/data-sync.ts` + admin-ops surfacing
- **Invocation 1 inline fixes verified:** `scripts/sync-external-listings.ts` preflight pointer; `lib/data-sync.ts` sync diagnostic includes `ccIngestSource`
- **Partner ingest:** CC API-first + scrape fallback; Phygitals honest stub (`PHYGITALS_LIVE_INGEST_AVAILABLE=false`); Tensor enrichment optional
- **Batch sync / cron:** `syncExternalListingsToJson` returns `ccIngestSource`, `dbUpsertCount`; `vercel.json` POST `/api/sync` every 30m
- **Aggregation:** `/trade/all` dedupe + venue tags via `lib/trade/all-listings.ts`; landing stats from partner ingest without Tensor key
- **Tests:** focused bundle started but host slow (>5m, no output yet); prior inv 1 reported partner-listings 28/28 pass

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G1 | Prod `ExternalListing` migration verify (P0-OPS-09 / P0-DAT-04) | P0 | operator / `npm run db:preflight` | deferred — operator |
| G2 | Confirm be-bg-1..5 test green after workers complete | P1 | tests/*partner*, tests/*sync* | deferred — await workers + QA lane |
| G9 | Phygitals live API ingest (P2-INT-PG03–06) | P2 | `lib/phygitals-listings.ts` | deferred — partner API unavailable |
| G10 | CC stable marketplace API (P2-INT-CC04–07) | P2 | `lib/collector-crypt-api-listings.ts` | deferred — partner stub |
| G11 | Magic Eden external listing ingest (P2-INT-D16) | P2 | aggregator Phase 4 | deferred |
| G12 | Grid/item deep-link CTA wiring | P1 | `components/trade/*` | deferred → **segment/UI lane** |

### Spawned workers

| Worker | Gap ID | Task title | Agent ID | Status |
|--------|--------|------------|----------|--------|
| — | — | — | — | **cap reached (concurrency cap)** — 0 spawns |

**Spawned count:** 0 (max 5 this iteration; global 25/24)  
**Inline fixes:** none (inv 1 inline fixes still valid; no trivial new gaps under cap)

### Iteration footer

- **Remaining gaps:** 6 — G1 prod DB preflight (P0); G2 confirm worker test green; G9–G11 partner/API deferred; G12 UI CTAs
- **Global in-flight after spawn:** 25/24 (unchanged)
- **Recommend next lane:** **qa** — confirmatory test pass once global cap drains; operator `npm run db:preflight` for G1
- **Stop reason:** LANE_ITERATIONS=1 complete + global cap reached

---

## End of invocation 2

**Total iterations:** 1  
**Total spawned this invocation:** 0 (cap reached)  
**Green criteria met:** no — 2 consecutive green meta ticks not met; prod DB preflight operator-owned  
**Operator action needed:**
- Drain global fix-worker cap (25 in-flight) before next backend spawns
- Run `npm run db:preflight` on production (P0-DAT-04)
- Re-run QA lane for confirmatory test + build after workers land

## Status: active

Global cap reached; invocation 1 workers (be-bg-1..5) code landed — pending QA confirmation and operator DB preflight.

---

## Invocation 3 — 2026-05-23T12:05:00Z

**Meta tick:** 3  
**Iteration:** 1 of 1  
**Elapsed:** ~12m  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path  
**Priority alignment:** (1) ExternalListing upsert hardening, (2) CC API-first ingest read+batch paths, (3) `/trade/all` aggregation BFF + dedupe

### Audit summary

- **Global in-flight fix workers (2026-05-23, before spawn):** **5** — segment seg-bg-25..29 (5). Inv1 backend/ui/onchain/security batches **drained** (code landed; segment post-verify **599 pass / 0 fail**). **Cap headroom: 19/24**
- **`DATABASE_URL`:** UNSET in orchestrator shell — JSON seed fallback only; prod `ExternalListing` upsert blocked until operator runs `npm run db:preflight` (P0-DAT-04 / P0-OPS-09)
- **Invocation 1 workers verified in tree:** deep-link BFF assertions, Phygitals `resolvePartnerDeepLink` tests, `fetchCollectorCryptIngestListings` API-first/scrape-fallback tests, `lastExternalListingsCcIngestSource` in `lib/data-sync.ts` + admin-ops
- **ExternalListing upsert:** `syncExternalListingsToJson` filters `activeForDb` before `upsertExternalListings`; Prisma upsert on `source_externalId` composite; inactive-row exclusion test **gap**
- **CC API-first ingest:** batch path tested (`external-listings-sync.test.ts`); read-path `fetchPartnerApiRows` in `aggregatePartnerExternalListings` when stale/empty — dedicated unit test **gap**
- **Batch sync / cron:** `vercel.json` POST `/api/sync` every 30m; sync diagnostic includes `ccIngestSource`; runbook preflight pointer in `scripts/sync-external-listings.ts`
- **Aggregation:** `loadAllTradeListings` → `mergeAllTradeListings` → `dedupePartnerTradeListings` + `buildAllListingsAggregateStats`; BFF `GET /api/trade/all/listings` returns `stats` but not deduped `aggregate` object — **gap**; `alternateVenueAsks` preserved in merge but BFF test coverage **gap**
- **Tensor:** optional enrichment only — core paths work without `TENSOR_API_KEY`
- **Tests (this invocation):**
  - Focused bundle: `partner-listings`, `external-listings-sync`, `api-trade-all-listings`, `trade-all-listings` — **72/72 pass** (~194s)

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G1 | Prod `ExternalListing` migration verify (P0-OPS-09 / P0-DAT-04) | P0 | operator / `npm run db:preflight` | deferred — operator |
| G13 | BFF missing deduped `aggregate` stats from `loadAllTradeListings` | P1 | `app/api/trade/all/listings/route.ts` | spawn be-bg-6 |
| G14 | BFF missing `alternateVenueAsks` assertion on cert dedupe merge | P1 | `tests/api-trade-all-listings.test.ts` | spawn be-bg-7 |
| G15 | Sync discover status detail missing ccIngestSource suffix test | P1 | `tests/data-sync-status.test.ts` | spawn be-bg-8 |
| G16 | ExternalListing upsert inactive-row exclusion untested | P1 | `tests/external-listings-sync.test.ts` | spawn be-bg-9 |
| G17 | CC API-first read path (`fetchPartnerApiRows`) unit test missing | P1 | `tests/partner-listings.test.ts` | spawn be-bg-10 |
| G9 | Phygitals live API ingest (P2-INT-PG03–06) | P2 | `lib/phygitals-listings.ts` | deferred — partner API unavailable |
| G10 | CC stable marketplace API (P2-INT-CC04–07) | P2 | `lib/collector-crypt-api-listings.ts` | deferred — partner stub |
| G11 | Magic Eden external listing ingest (P2-INT-D16) | P2 | aggregator Phase 4 | deferred |
| G12 | Grid/item deep-link CTA wiring | P1 | `components/trade/*` | deferred → **segment/UI lane** |

### Spawned workers

| Worker | Gap ID | Task title | Agent ID | Status |
|--------|--------|------------|----------|--------|
| be-bg-6 | G13 | Backend — BFF aggregate stats exposure | `92a5d78d-776e-4e8a-a09f-2e01da82462e` | spawned (not awaited) |
| be-bg-7 | G14 | Backend — all-listings alternateVenueAsks test | `7c8a30f4-dd09-4a48-a03b-624d86cec3a8` | spawned (not awaited) |
| be-bg-8 | G15 | Backend — sync ccIngestSource status test | `4d67daae-9f71-4eb8-9bbd-43edc7df78da` | spawned (not awaited) |
| be-bg-9 | G16 | Backend — inactive row upsert exclusion test | `ce7bd130-9bb9-4ae3-8454-9a9e43024605` | spawned (not awaited) |
| be-bg-10 | G17 | Backend — CC API-first read path test | `88e47a8b-4719-4594-9c08-272e91bf8f50` | spawned (not awaited) |

**Spawned count:** 5 (max 5 this iteration; global cap 24 — 5 in-flight + 5 = 10)  
**Inline fixes:** none (no trivial 1–5 line gaps under focus)

### Iteration footer

- **Remaining gaps:** 5 deferred (G1, G9–G12) + 5 in-flight workers (be-bg-6..10)
- **Global in-flight after spawn:** 10/24
- **Recommend next lane:** **qa** (confirmatory pass once workers land) or **operator** (`npm run db:preflight` for G1)
- **Stop reason:** LANE_ITERATIONS=1 complete; do not await workers

---

## End of invocation 3

**Total iterations:** 1  
**Total spawned this invocation:** 5 (be-bg-6..10, not awaited)  
**Green criteria met:** partial — focused ingest/aggregation tests 72/72 pass; BFF aggregate + read-path API-first coverage queued to workers; prod DB preflight operator-owned  
**Operator action needed:**
- Run `npm run db:preflight` on production (P0-DAT-04)
- Review be-bg-6..10 diffs when complete
- No commit unless operator approved

## Status: active

Pending worker output for BFF aggregate stats, alternateVenueAsks tests, sync ccIngestSource status test, inactive upsert exclusion, and CC API-first read path test.

### Post-run test confirmation — 2026-05-23T11:28:47Z

- `npm test` (backend lane bundle invocation) — **599/599 pass**, exit 0 (~33m)
- Resolves prior 598/599 flake from concurrent worker window; be-bg-1..5 coverage appears green

---

## Invocation 4 — 2026-05-23T12:35:00Z

**Meta tick:** 4  
**Iteration:** 1 of 1  
**Elapsed:** ~12m  
**North star:** #1 collector platform — cert → all listings → price signals → buy on best path  
**Priority alignment:** (1) ExternalListing upsert + preflight, (2) CC API-first ingest read+batch, (3) `/trade/all` aggregation BFF + dedupe

### Audit summary

- **Global in-flight fix workers (2026-05-23, before spawn):** **12** — segment seg-bg-25..29 (5), onchain oc-bg-6..10 (5), backend be-bg-6 + be-bg-10 (2; inv3 be-bg-7..9 **landed**). **Cap headroom: 12/24**
- **`DATABASE_URL`:** UNSET in orchestrator shell — JSON seed fallback only; prod `ExternalListing` upsert blocked until operator runs `npm run db:preflight` (P0-DAT-04 / P0-OPS-09)
- **Inv3 worker landings (code review):** be-bg-7 (alternateVenueAsks BFF test), be-bg-8 (ccIngestSource status tests), be-bg-9 (inactive-row upsert exclusion) **verified in tree**; be-bg-6 (BFF `aggregate` field) and be-bg-10 (CC API-first read path) **still open**
- **ExternalListing upsert:** `activeForDb` filter + sold/unknown exclusion tested; DB upsert **failure** path lacks sync-module unit test (admin-ops integration only)
- **CC ingest:** batch API-first/scrape-fallback green; read-path `fetchPartnerApiRows` when stale/empty still untested; `partnerIngestNeedsRefresh` untested; cached `ccIngestSource` not asserted in sync test
- **Aggregation:** `loadAllTradeListings` returns `aggregate`; BFF `GET /api/trade/all/listings` still omits `aggregate` (only `stats`, `venueCount`, `venues`)
- **Batch sync / cron:** `vercel.json` POST `/api/sync` every 30m; discover detail ccIngestSource suffix for **api** tested — **scrape/cached** suffix gap
- **Tensor:** optional enrichment only — core paths work without `TENSOR_API_KEY`
- **Tests (this invocation):**
  - Focused bundle: `partner-listings`, `external-listings-sync`, `api-trade-all-listings`, `trade-all-listings`, `data-sync-status` — **82/82 pass** (~402s)

### Gaps

| ID | Gap | Severity | Owner files | Action |
|----|-----|----------|-------------|--------|
| G1 | Prod `ExternalListing` migration verify (P0-OPS-09 / P0-DAT-04) | P0 | operator / `npm run db:preflight` | deferred — operator |
| G13 | BFF missing deduped `aggregate` stats from `loadAllTradeListings` | P1 | `app/api/trade/all/listings/route.ts` | in-flight be-bg-6 |
| G17 | CC API-first read path (`fetchPartnerApiRows`) unit test missing | P1 | `tests/partner-listings.test.ts` | in-flight be-bg-10 |
| G18 | Sync DB upsert failure not unit-tested in external-listings-sync | P1 | `tests/external-listings-sync.test.ts` | spawn be-bg-11 |
| G19 | Discover status ccIngestSource suffix missing scrape/cached tests | P1 | `tests/data-sync-status.test.ts` | spawn be-bg-12 |
| G20 | Cached CC ingest source not asserted when scrape returns empty | P1 | `tests/external-listings-sync.test.ts` | spawn be-bg-13 |
| G21 | Beezie/Courtyard preview adapter honest stub untested | P2 | `lib/partner-ingest-adapter.ts`, tests | spawn be-bg-14 |
| G22 | `partnerIngestNeedsRefresh` unit coverage missing | P1 | `lib/partner-listings.ts`, tests | spawn be-bg-15 |
| G9 | Phygitals live API ingest (P2-INT-PG03–06) | P2 | `lib/phygitals-listings.ts` | deferred — partner API unavailable |
| G10 | CC stable marketplace API (P2-INT-CC04–07) | P2 | `lib/collector-crypt-api-listings.ts` | deferred — partner stub |
| G11 | Magic Eden external listing ingest (P2-INT-D16) | P2 | aggregator Phase 4 | deferred |
| G12 | Grid/item deep-link CTA wiring | P1 | `components/trade/*` | deferred → **segment/UI lane** |

### Spawned workers

| Worker | Gap ID | Task title | Agent ID | Status |
|--------|--------|------------|----------|--------|
| be-bg-11 | G18 | Backend — sync DB upsert failure unit test | `23c113a9-f262-4322-be14-64feb5363c66` | spawned (not awaited) |
| be-bg-12 | G19 | Backend — scrape/cached ccIngestSource status tests | `72c5e746-2791-4f93-ba07-81ec66142897` | spawned (not awaited) |
| be-bg-13 | G20 | Backend — cached ccIngestSource sync assertion | `4623db00-142e-426b-8694-9cc110e98efb` | spawned (not awaited) |
| be-bg-14 | G21 | Backend — preview adapter honest stub tests | `c8fdacbe-5736-4d3b-b73a-e91cac0d2875` | spawned (not awaited) |
| be-bg-15 | G22 | Backend — partnerIngestNeedsRefresh unit tests | `978f856e-83f3-4acc-b647-6d266731c652` | spawned (not awaited) |

**Spawned count:** 5 (max 5 this iteration; global cap 24 — 12 in-flight + 5 = 17)  
**Inline fixes:** none (no trivial 1–5 line gaps under focus; G13 route change deferred to be-bg-6)

### Iteration footer

- **Remaining gaps:** 6 deferred (G1, G9–G12) + 2 in-flight inv3 (be-bg-6, be-bg-10) + 5 new in-flight (be-bg-11..15)
- **Global in-flight after spawn:** 17/24
- **Recommend next lane:** **qa** (confirmatory pass once worker batch lands) or **operator** (`npm run db:preflight` for G1)
- **Stop reason:** LANE_ITERATIONS=1 complete; do not await workers

---

## End of invocation 4

**Total iterations:** 1  
**Total spawned this invocation:** 5 (be-bg-11..15, not awaited)  
**Green criteria met:** partial — focused ingest/aggregation tests 82/82 pass; BFF aggregate + CC read-path API-first still in-flight from inv3; new sync/adapter test coverage queued  
**Operator action needed:**
- Run `npm run db:preflight` on production (P0-DAT-04)
- Review be-bg-6, be-bg-10..15 diffs when complete
- No commit unless operator approved

## Status: active

Pending worker output for BFF aggregate exposure (be-bg-6), CC API-first read path (be-bg-10), sync failure/cached ingest tests (be-bg-11..13), preview adapter stubs (be-bg-14), and partnerIngestNeedsRefresh (be-bg-15).
