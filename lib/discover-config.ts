/**
 * Custom `/discover` deep-link aggregator — ARCHIVED (May 2026).
 * Public lane removed; `/trade` is the sole purchase surface.
 * Sync scripts (`npm run sync:discover`) and schema remain for operator tooling only.
 * See docs/discover-setup.md (ARCHIVED).
 */
export function isDiscoverAggregatorEnabled(): boolean {
  return false;
}
