[package]
name = "slabvault-broker"
version = "0.1.0"
description = "SlabVault broker overlay — composes with Tensor TCM + fees (devnet scaffold)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "slabvault_broker"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.29.0"
